---
phase: 04-contract-hardening
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - SECURITY.md
  - .planning/phases/04-contract-hardening/slither-attempt.log
  - .planning/phases/04-contract-hardening/security-review.md
autonomous: true
requirements: [HARD-03]
must_haves:
  truths:
    - "A static-analysis attempt is made (slither via pip) and its outcome — success with findings, or failure with documented reason — is recorded"
    - "If slither is not installable in this sandbox, the attempt is documented and a manual SWC checklist is run instead"
    - "A security-reviewer agent pass over Cascade.sol + the AA contracts is complete"
    - "Every CRITICAL/HIGH finding is resolved; every accepted MEDIUM/LOW carries explicit rationale"
    - "Findings + the honest hackathon-grade-not-audit caveat live in a committed SECURITY.md"
  artifacts:
    - path: "SECURITY.md"
      provides: "Static-analysis + manual review findings, dispositions, audit caveat"
      contains: "## Findings"
    - path: ".planning/phases/04-contract-hardening/slither-attempt.log"
      provides: "Recorded slither install/run attempt (success or documented failure)"
      min_lines: 1
    - path: ".planning/phases/04-contract-hardening/security-review.md"
      provides: "Captured security-reviewer agent finding list"
      min_lines: 1
  key_links:
    - from: "SECURITY.md"
      to: "src/Cascade.sol + src/aa/*.sol"
      via: "per-finding file:line references"
      pattern: "Cascade\\.sol|CascadeAccount\\.sol"
---

<objective>
Run static analysis (slither if installable, documented fallback if not) plus a structured security-reviewer pass over the hardened Cascade + AA contracts, resolve every CRITICAL/HIGH finding, and record everything — including the honest "review-ready, not an independent audit" caveat — in a committed SECURITY.md.

Purpose: HARD-03, the final publish-ready gate. A reviewer (and the Phase 5 mainnet decision) needs evidence the contracts were analyzed against known vulnerability classes and that any residual risk is explicitly accepted with rationale, not silently ignored.

Output: `SECURITY.md`, a recorded slither attempt log, a captured security-review finding list, and any fixes for CRITICAL/HIGH findings (which must keep the full suite green).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-contract-hardening/04-CONTEXT.md
@.planning/phases/04-contract-hardening/04-01-SUMMARY.md
@.planning/phases/04-contract-hardening/04-02-SUMMARY.md

<interfaces>
<!-- Sandbox tooling (confirmed at plan time): forge + cast present; `pip` present; -->
<!-- slither + pipx NOT present. slither also needs solc (forge ships solc internally -->
<!-- but slither expects a solc on PATH or via solc-select). Treat install as best-effort. -->
<!-- The 04-CONTEXT explicitly permits a documented fallback to the manual SWC checklist -->
<!-- if slither is not installable in this environment. -->

<!-- Contracts under review (post Plan 01/02 hardening): -->
<!--   src/Cascade.sol — register/invoke/claim/_distribute; pull-payment; CEI in claim; -->
<!--     monotonic-id cycle prevention; depth cap 8; per-level remainder conservation. -->
<!--   src/aa/CascadeAccount.sol — validateUserOp (inline ecrecover + EIP-2 high-s guard; -->
<!--     returns 0/1 never reverts on sig mismatch); execute/executeBatch; _call asm bubble-up; -->
<!--     _payPrefund ignores call success by design (EntryPoint enforces). -->
<!--   src/aa/AccountFactory.sol — CREATE2 idempotent createAccount; getAddress. -->
<!--   src/aa/{IEntryPoint,IAccount,PackedUserOperation}.sol — interfaces. -->
<!-- Prior security-relevant decisions (STATE.md) to reference, not re-litigate: -->
<!--   pull-payment over push; CEI ordering in claim; reentrancy-safe fan-out (no external -->
<!--   calls during _distribute); validateUserOp must NOT revert on sig mismatch. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Attempt slither; on failure document it and run the manual SWC checklist</name>
  <read_first>
    - src/Cascade.sol, src/aa/CascadeAccount.sol, src/aa/AccountFactory.sol
    - .planning/phases/04-contract-hardening/04-CONTEXT.md (HARD-03 slither-or-fallback clause)
  </read_first>
  <action>
    Best-effort install + run slither, capturing ALL output (stdout+stderr) to .planning/phases/04-contract-hardening/slither-attempt.log:
    - Try `pip install slither-analyzer` (no pipx in this env). If solc resolution is needed, note that forge bundles solc; try `slither . --solc-remaps @forge-std/=lib/forge-std/` or point slither at the foundry profile. Do NOT add network deps to lib/.
    - If install or run fails (no network, solc mismatch, etc.), record the exact command(s) + error in the log — this documented attempt satisfies the CONTEXT clause.
    Then, whether slither ran or not, perform a manual SWC checklist pass and capture per-item notes (for SECURITY.md Task 3). Cover at minimum: reentrancy (SWC-107) — confirm claim CEI + no external calls in _distribute; integer over/underflow (SWC-101) — 0.8.x checked arithmetic + the bound on shares/depth; unchecked low-level call return (SWC-104) — claim's TransferFailed revert vs _payPrefund's intentional ignore (document the rationale); tx.origin (SWC-115) — confirm absent; uninitialized storage / shadowing; DoS via unbounded loop (SWC-128) — depth cap 8 + register-time bound; access control (SWC-105/106) — onlyEntryPointOrOwner + validateUserOp EntryPoint guard; signature malleability (SWC-117) — EIP-2 high-s guard in _recover; floating/locked pragma (SWC-103) — note ^0.8.24.
    Do not change contract logic in this task; only gather evidence.
  </action>
  <acceptance_criteria>
    - `.planning/phases/04-contract-hardening/slither-attempt.log` exists and is non-empty.
    - The log records EITHER a successful slither run + its raw findings, OR the exact failing command(s) and error output (documented failure).
    - The manual SWC checklist notes cover at minimum SWC-101, 104, 105/106, 107, 115, 117, 128, 103 with a per-item conclusion.
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/04-contract-hardening/slither-attempt.log</automated>
  </verify>
  <done>slither-attempt.log exists and records either a successful slither run + its findings, or the install/run failure with the exact command and error. A manual SWC checklist has been worked through with per-item notes captured for SECURITY.md.</done>
</task>

<task type="auto">
  <name>Task 2: security-reviewer agent pass over Cascade + AA</name>
  <read_first>
    - src/Cascade.sol, src/aa/CascadeAccount.sol, src/aa/AccountFactory.sol
    - src/aa/IEntryPoint.sol, src/aa/IAccount.sol, src/aa/PackedUserOperation.sol
    - .planning/phases/04-contract-hardening/slither-attempt.log (Task 1 evidence)
  </read_first>
  <action>
    Invoke the security-reviewer agent against src/Cascade.sol and src/aa/*.sol (automated, no human gate — local review, no funds, no irreversible action). The agent must produce a categorized finding list (CRITICAL/HIGH/MEDIUM/LOW), each with a file:line reference, covering at minimum: reentrancy, access control (onlyEntryPointOrOwner + EntryPoint-only validateUserOp), arithmetic, low-level call return handling (claim vs _payPrefund), signature handling (EIP-2 high-s guard, validateUserOp-never-reverts contract), and CREATE2/initCode determinism in AccountFactory. Capture the agent's finding list verbatim into .planning/phases/04-contract-hardening/security-review.md so Task 3 can triage it. Do not fix anything in this task — gather findings only.
  </action>
  <acceptance_criteria>
    - The security-reviewer agent has been run against src/Cascade.sol + src/aa/*.sol.
    - `.planning/phases/04-contract-hardening/security-review.md` exists and is non-empty, with each finding carrying a severity and a file:line reference.
    - Findings span reentrancy, access control, arithmetic, low-level calls, signature handling, and CREATE2 determinism (or explicitly note "no finding" per class).
  </acceptance_criteria>
  <verify>
    <automated>test -s .planning/phases/04-contract-hardening/security-review.md && grep -qiE 'severity|CRITICAL|HIGH|MEDIUM|LOW' .planning/phases/04-contract-hardening/security-review.md</automated>
  </verify>
  <done>The security-reviewer finding list (severity + file:line per item) is captured in security-review.md and ready to triage in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve CRITICAL/HIGH, document the rest, write SECURITY.md</name>
  <read_first>
    - .planning/phases/04-contract-hardening/slither-attempt.log (Task 1 evidence)
    - .planning/phases/04-contract-hardening/security-review.md (Task 2 findings)
    - src/Cascade.sol, src/aa/CascadeAccount.sol, src/aa/AccountFactory.sol
    - .planning/phases/04-contract-hardening/04-CONTEXT.md (HARD-03 resolution + honesty requirements)
  </read_first>
  <action>
    Triage every finding from Task 1 (slither/SWC) + Task 2 (security-reviewer). For each CRITICAL or HIGH: fix it in the contract (logic change permitted ONLY if it does not alter the frozen external ABI — re-run the full suite + fork test after any fix). For each accepted MEDIUM/LOW: write explicit rationale (e.g. _payPrefund intentionally ignores call success because the EntryPoint enforces the prefund; per-level remainder credited to creator by design; account-agnostic by design).
    Write SECURITY.md at repo root with:
    - A short scope/methodology section (slither attempt outcome + manual SWC checklist + security-reviewer pass).
    - A `## Findings` section: a table of every finding with id, severity, location (file:line), status (Resolved / Accepted), and disposition/rationale.
    - The design-decision security notes (pull-payment, CEI, reentrancy-safe fan-out, validateUserOp-never-reverts, EIP-2 malleability guard, depth cap, monotonic-id cycle prevention).
    - An honest caveat: this is hackathon-grade review-readiness, NOT an independent professional audit; an independent audit is the one remaining step before custody of real user value.
    If any CRITICAL/HIGH fix touched contract code, re-run full + fork suites and (if gas changed) re-run `forge snapshot --no-match-path 'test/*.fork.t.sol'` to keep .gas-snapshot current.
  </action>
  <acceptance_criteria>
    - SECURITY.md exists at repo root with a `## Findings` table (id, severity, file:line, status, rationale).
    - No CRITICAL or HIGH finding is left Open/Unresolved/TODO in the table.
    - SECURITY.md contains the explicit not-an-independent-audit / hackathon-grade caveat.
    - `forge test --no-match-path 'test/*.fork.t.sol'` is green; if any contract fix was made, `forge test --fork-url atlantic_testnet --match-path 'test/*.fork.t.sol'` is also green and `.gas-snapshot` was refreshed.
  </acceptance_criteria>
  <verify>
    <automated>test -s SECURITY.md && grep -q '## Findings' SECURITY.md && grep -qiE 'not an independent|not a (professional )?audit|hackathon-grade' SECURITY.md && ! grep -qiE '^\|.*\b(CRITICAL|HIGH)\b.*\b(Open|Unresolved|TODO)\b' SECURITY.md && forge test --no-match-path 'test/*.fork.t.sol' 2>&1 | grep -qE 'Suite result: ok'</automated>
  </verify>
  <done>SECURITY.md exists with a Findings table, the audit caveat, and design notes; no CRITICAL/HIGH finding is left Open/Unresolved; the full local suite is still green (and .gas-snapshot refreshed if any fix changed gas).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| analysis tooling -> environment | slither install may fail offline; the fallback must still produce a documented review, not a gap |
| reviewer findings -> contract fixes | A HIGH fix must not breach the frozen ABI; the suite + fork test are the regression gate |
| native value -> claim / _payPrefund | Low-level calls; one must revert on failure (claim), one intentionally ignores (prefund) — the distinction must be documented, not accidental |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Repudiation | undocumented residual risk | mitigate | SECURITY.md records every finding + disposition + the not-an-audit caveat |
| T-04-08 | Elevation of privilege | access control on execute/validateUserOp | mitigate | Review confirms onlyEntryPointOrOwner + EntryPoint-only validateUserOp; any gap is CRITICAL/HIGH and fixed in Task 3 |
| T-04-09 | Tampering | a HIGH fix silently changing ABI/semantics | mitigate | Fixes re-run full + fork suite; ABI frozen per Plan 01 invariant |
| T-04-SC | Tampering | pip install slither-analyzer (supply chain) | mitigate | Install confined to the analysis tool in a local venv/env; no addition to lib/ (stays forge-std only); attempt logged |
</threat_model>

<verification>
- `.planning/phases/04-contract-hardening/slither-attempt.log` exists (run or documented failure)
- `.planning/phases/04-contract-hardening/security-review.md` captured + triaged
- SECURITY.md exists with `## Findings`, the not-an-audit caveat, no Open CRITICAL/HIGH
- Full local suite green after any fix
</verification>

<success_criteria>
Static analysis attempted + recorded; manual SWC checklist + security-reviewer pass complete; every CRITICAL/HIGH resolved; accepted findings documented with rationale in a committed SECURITY.md carrying the honest audit caveat; suite stays green.
</success_criteria>

<output>
Create `.planning/phases/04-contract-hardening/04-03-SUMMARY.md` when done.
</output>
