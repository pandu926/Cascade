---
phase: 05-mainnet-deployment
plan: 03
type: execute
wave: 3
depends_on: ["05-01", "05-02"]
files_modified: [MAINNET_RESULT.md, script/LiveBundleMainnet.s.sol]
autonomous: false
requirements: [MAIN-03]
user_setup: []

must_haves:
  truths:
    - "An AccountFactory + a CascadeAccount are deployed on Pharos mainnet (chainId 1672), proving AA-01 live"
    - "ONE self-bundled handleOps via the real EntryPoint v0.7 batches TWO Cascade.invoke calls in a single tx"
    - "Both invoked skills' creator balances rise as a result of the single UserOp (account-agnostic parity with the EOA path)"
    - "Every deploy/funding/handleOps tx hash, before/after balances, and explorer links are recorded in MAINNET_RESULT.md"
    - "A final human-verify checkpoint presents all mainnet artifacts (verified Cascade, royalty demo, 4337 demo) for explorer confirmation"
  artifacts:
    - path: "script/LiveBundleMainnet.s.sol"
      provides: "Mainnet-guarded (chainId 1672) self-bundled 4337 batch driver: factory deploy + handleOps of two invokes"
      contains: "handleOps"
    - path: "MAINNET_RESULT.md"
      provides: "Complete mainnet record: 4337 addresses, UserOp tx hash, balance deltas, explorer links"
      contains: "handleOps"
  key_links:
    - from: "EOA handleOps([userOp]) on real EntryPoint v0.7"
      to: "two Cascade.invoke calls via executeBatch"
      via: "PackedUserOperation callData = executeBatch of two invokes"
      pattern: "ep.handleOps"
---

<objective>
Reproduce the Phase 3 fork-proven ERC-4337 flow LIVE on Pharos mainnet: deploy an AccountFactory + CascadeAccount, fund the account minimally, then submit ONE self-bundled `handleOps` through the real EntryPoint v0.7 whose UserOp batches TWO `Cascade.invoke` calls in a single transaction. Then present every mainnet artifact for human verification.

Purpose: Satisfies MAIN-03 — a real mainnet UserOp batching two invokes, linkable on pharosscan, proving the account-agnostic claim with real money. The final checkpoint closes the phase by confirming all three headline artifacts on the explorer.
Output: 4337 addresses + UserOp tx hash + balance deltas + explorer links appended to MAINNET_RESULT.md; a new mainnet-guarded driver script; a human-verify checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mainnet-deployment/05-CONTEXT.md
@.planning/phases/03-smart-account-agents-erc-4337/03-RESEARCH.md
@.planning/phases/05-mainnet-deployment/05-01-SUMMARY.md
@.planning/phases/05-mainnet-deployment/05-02-SUMMARY.md

<interfaces>
<!-- script/LiveBundle.s.sol is the Phase 3 fork-proven driver — but it is HARDCODED to testnet -->
<!-- (ATLANTIC_CHAIN_ID=688689, CASCADE_ADDR=testnet, and require(chainid==ATLANTIC) REFUSES mainnet). -->
<!-- Reuse its EXACT v0.7 build/sign/handleOps mechanics; create a mainnet-guarded sibling rather than -->
<!-- weakening that frozen testnet safety guard. -->

From script/LiveBundle.s.sol (mechanics to mirror verbatim):
  EP_ADDR = 0x0000000071727De22E5E9d8BAf0edAc6f37da032   // EntryPoint v0.7 — SAME canonical singleton on mainnet (orchestrator-confirmed, 32KB bytecode)
  Pre-flight gate: opGas*gasPrice prefund + invoke values + EOA deploy/handleOps gas vs balance; STOP CLEANLY on shortfall.
  _buildSignedOp: nonce=ep.getNonce(sender,0), initCode="", callData=executeBatch, gasFees packed maxFee==maxPriority (legacy-safe),
    sign keccak256("\x19Ethereum Signed Message:\n32" || ep.getUserOpHash(op)) with owner key, signature=abi.encodePacked(r,s,v).
  _batchInvoke: executeBatch([cascade,cascade],[p1,p2],[invoke(id1),invoke(id2)]).
  _pack(high,low) = bytes32((high<<128)|low).

From src/aa/AccountFactory.sol:
  constructor(IEntryPoint _entryPoint);
  function createAccount(address owner, uint256 salt) public returns (address);   // idempotent CREATE2
  function getAddress(address owner, uint256 salt) public view returns (address);  // counterfactual

From src/aa/CascadeAccount.sol:
  constructor(IEntryPoint anEntryPoint, address anOwner);
  function executeBatch(address[] dest, uint256[] value, bytes[] func) external onlyEntryPointOrOwner;
  receive() external payable {}   // accept funding

From src/Cascade.sol:
  function invoke(uint256 skillId) external payable;   // requires msg.value == price exactly
  mapping(address => uint256) public balances;
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create mainnet-guarded 4337 driver; deploy factory + account (pre-flight gated)</name>
  <files>script/LiveBundleMainnet.s.sol, MAINNET_RESULT.md</files>
  <read_first>
    - script/LiveBundle.s.sol (the proven build/sign/handleOps mechanics + pre-flight gate to mirror; note the hardcoded testnet constants + the require(chainid==ATLANTIC) guard that REFUSES mainnet)
    - .planning/phases/03-smart-account-agents-erc-4337/03-RESEARCH.md (v0.7 UserOp packing/hashing — authoritative)
    - src/aa/AccountFactory.sol, src/aa/CascadeAccount.sol (factory createAccount + account executeBatch/receive)
    - .planning/phases/05-mainnet-deployment/05-CONTEXT.md (prefer factory over direct deploy since budget ample — proves AA-01 on mainnet; fund account for prefund + 2 invoke values; reuse MAIN-02 skills or register a fresh pair; NEVER print PRIVATE_KEY)
    - .planning/phases/05-mainnet-deployment/05-01-SUMMARY.md + 05-02-SUMMARY.md (mainnet Cascade address + registered skill ids)
  </read_first>
  <action>
    Create `script/LiveBundleMainnet.s.sol` mirroring LiveBundle.s.sol's EXACT v0.7 mechanics (_buildSignedOp, _batchInvoke, _pack, the pre-flight budget gate) with these mainnet differences — a NEW file, leaving the frozen testnet LiveBundle and its testnet-only safety guard untouched (immutability + don't weaken a safety guard):
    - Guard `require(block.chainid == 1672, "wrong chainId: refusing to broadcast")` (mainnet, NOT atlantic).
    - Read the Cascade address from env `CASCADE` (the 05-01 deployment) — do not hardcode.
    - EP_ADDR stays 0x0000000071727De22E5E9d8BAf0edAc6f37da032 (same canonical EntryPoint v0.7 on mainnet).
    - DEPLOY VIA THE FACTORY (CONTEXT preference, proves AA-01 on mainnet): `new AccountFactory(ep)` then `factory.createAccount(broadcaster, salt)`; the account owner = broadcaster EOA (so the broadcaster key signs valid UserOps). Record both factory + account addresses.
    - The two skills to batch: reuse the MAIN-02 skills from 05-02 (e.g. idA price 0 + idC price 0.001), OR register a fresh small pair — read ids/prices from env (SKILL_ID_1/2, PRICE_1/2). Cascade enforces msg.value==price on-chain as the fail-safe.
    - gasFees packed legacy-safe (maxFee==maxPriority==10 gwei); pass `--legacy --with-gas-price 10000000000` at run time too.

    PRE-FLIGHT GATE inside run() (mirrors LiveBundle — runs FIRST, STOP CLEANLY/revert with no broadcast on shortfall): estimate = EOA gas (factory deploy + createAccount + handleOps) + account native value (prefund + price1 + price2), compared to deployer balance. Budget is ample (~2 PROS vs ~0.04 expected) but still gate.

    NORMALIZE KEY WITHOUT PRINTING: `PK=0x${PRIVATE_KEY#0x}` (never echoed). Confirm `cast chain-id --rpc-url mainnet` == 1672 before running.

    Run the deploy portion (or the whole script in one broadcast — implementer's choice, but record each resulting tx). RECORD to MAINNET_RESULT.md (new "MAIN-03 4337 Demo" section): factory address, account address, the funding tx, before balances, and explorer links.
  </action>
  <acceptance_criteria>
    - `script/LiveBundleMainnet.s.sol` exists and contains `require(block.chainid == 1672` and `ep.handleOps` and reads `CASCADE` from env (no hardcoded mainnet Cascade).
    - `cast chain-id --rpc-url mainnet` == 1672 confirmed before broadcast.
    - `cast code <factory_addr> --rpc-url mainnet` and `cast code <account_addr> --rpc-url mainnet` both return more than `0x` (deployed).
    - `cast call <factory> "getAddress(address,uint256)(address)" <owner> <salt> --rpc-url mainnet` equals the deployed account address (CREATE2 determinism holds).
    - The funding tx `cast receipt` status == 1; account balance (`cast balance <account>`) covers prefund + the two invoke values.
    - MAINNET_RESULT.md records factory address, account address, funding tx hash, and explorer links. No key/mnemonic leaked.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Build + sign + self-bundle ONE handleOps batching two invokes; record deltas</name>
  <files>MAINNET_RESULT.md</files>
  <read_first>
    - script/LiveBundleMainnet.s.sol (the driver created in Task 1)
    - script/LiveBundle.s.sol (the proven _buildSignedOp/_batchInvoke/handleOps flow it mirrors)
    - .planning/phases/03-smart-account-agents-erc-4337/03-RESEARCH.md (v0.7 hashing/packing — must match the real EntryPoint's getUserOpHash)
    - src/Cascade.sol (invoke requires exact msg.value; balances getter)
    - .planning/phases/05-mainnet-deployment/05-CONTEXT.md (assert balances rise; record tx hashes + links; reproduces the Phase 3 fork-proven flow live)
  </read_first>
  <action>
    Run the handleOps portion of `script/LiveBundleMainnet.s.sol` on mainnet: it builds ONE PackedUserOperation whose callData = `executeBatch([cascade,cascade],[price1,price2],[invoke(id1),invoke(id2)])`, signs it with the account owner key over `ep.getUserOpHash(op)` (eth-signed-prefixed), and the EOA calls `ep.handleOps([op], payable(broadcaster))` — self-bundled, no external bundler.

    Command shape: `CASCADE=<ADDR> SKILL_ID_1=<id1> SKILL_ID_2=<id2> PRICE_1=<p1> PRICE_2=<p2> forge script script/LiveBundleMainnet.s.sol:LiveBundleMainnetScript --rpc-url mainnet --broadcast --legacy --with-gas-price 10000000000 -vvv`. (If the prior phase's forge-script gas-handling symptom recurs, the pre-flight gate prevents a half-spend; diagnose before retrying.)

    Capture balances of the two invoked skills' creators BEFORE and AFTER via `cast call <CASCADE> "balances(address)(uint256)" <creator> --rpc-url mainnet`. Confirm the single handleOps tx settled (status 1) and both creators rose.

    RECORD to MAINNET_RESULT.md: the handleOps tx hash, the two batched skill ids + prices, before/after balances of the affected creators, the deltas, and the explorer link `https://www.pharosscan.xyz/tx/<handleOps_tx>`. Note explicitly that ONE tx settled TWO invokes through the real EntryPoint v0.7 (account-agnostic parity with the MAIN-02 EOA path).
  </action>
  <acceptance_criteria>
    - Exactly ONE `handleOps` tx was broadcast; `cast receipt <handleOps_tx> --rpc-url mainnet` status == 1.
    - The tx emitted EntryPoint UserOperationEvent and BOTH Cascade Invoked events (two invokes settled in one tx) — confirmable via `cast receipt`/`cast logs`.
    - Both invoked skills' creator balances rose (`cast call balances(address)` after > before) as a result of the single UserOp.
    - MAINNET_RESULT.md records the handleOps tx hash, both skill ids, before/after balances + deltas, and the explorer link.
    - No private key/mnemonic leaked to stdout, logs, or the record file.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All three Phase 5 mainnet headline artifacts are now live and recorded in MAINNET_RESULT.md:
    1. MAIN-01: Cascade deployed + (best-effort) source-verified on pharosscan.
    2. MAIN-02: one live invoke of an A→B→C tree paid three creators proportionally (Σ == 0.001 PROS).
    3. MAIN-03: one self-bundled handleOps batched two invokes through the real EntryPoint v0.7.
  </what-built>
  <how-to-verify>
    1. Open MAINNET_RESULT.md and confirm it contains: the Cascade address, deploy tx, verification status, the three creator addresses + funding/register/invoke txs + RoyaltyAccrued amounts, and the factory/account addresses + handleOps tx.
    2. Visit `https://www.pharosscan.xyz/address/<CASCADE_ADDR>` — confirm the contract exists and (if verification succeeded) the Contract tab shows verified/readable source. If verification is recorded as best-effort, confirm that note is honest.
    3. Visit `https://www.pharosscan.xyz/tx/<MAIN-02_invoke_tx>` — confirm one invoke tx with three RoyaltyAccrued events.
    4. Visit `https://www.pharosscan.xyz/tx/<MAIN-03_handleOps_tx>` — confirm one handleOps tx that settled two invokes via the EntryPoint.
    5. Spot-check on-chain: `cast call <CASCADE> "skillCount()(uint256)" --rpc-url mainnet` is non-zero; the recorded creator balances match `cast call <CASCADE> "balances(address)(uint256)" <creator> --rpc-url mainnet`.
  </how-to-verify>
  <resume-signal>Type "approved" once the explorer links and on-chain state match MAINNET_RESULT.md, or describe any discrepancy.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → mainnet RPC | factory/account deploy + funding + handleOps broadcasts cross here; irreversible |
| EOA owner key → CascadeAccount | the broadcaster key signs the UserOp the account validates |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Information Disclosure | PRIVATE_KEY (owner/signer) | mitigate | Normalize in shell var, never echo; never write to MAINNET_RESULT.md |
| T-05-10 | Tampering/misconfig | broadcasting to wrong chain | mitigate | New driver hard-guards require(block.chainid==1672); cast chain-id confirmed pre-broadcast |
| T-05-11 | Denial of Service | half-spend on multi-step 4337 flow | mitigate | Pre-flight gate (deploy+createAccount+handleOps gas + prefund + 2 invoke values) STOPS cleanly on shortfall |
| T-05-12 | Spoofing | forged UserOp signature | accept | EntryPoint validates owner ECDSA via account validateUserOp (returns 1 on mismatch → AA24); wrong sig fails safe, no spend of invoke value |
| T-05-13 | Repudiation | unrecorded irreversible spend | mitigate | Every tx hash + before/after balances + explorer links recorded in MAINNET_RESULT.md before checkpoint |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | N/A — no package installs this phase |
</threat_model>

<verification>
- `script/LiveBundleMainnet.s.sol` hard-guards chainId 1672 and reads CASCADE from env.
- `cast chain-id --rpc-url mainnet` == 1672 (pre-broadcast).
- factory + account `cast code` non-empty; factory getAddress == deployed account (CREATE2 determinism).
- ONE handleOps `cast receipt` status == 1, with two Invoked events; both creator balances rose.
- MAINNET_RESULT.md records all 4337 addresses, the handleOps tx hash, deltas, explorer links; human-verify checkpoint passed.
</verification>

<success_criteria>
A live mainnet smart account batched two invokes in one self-bundled handleOps via the real EntryPoint v0.7, raising both creators' balances; every spend was pre-flight gated and recorded; a final human-verify checkpoint confirmed all three Phase 5 artifacts on the explorer. (Phase success criteria 3 + 4.)
</success_criteria>

<output>
Create `.planning/phases/05-mainnet-deployment/05-03-SUMMARY.md` when done, including the factory/account addresses + the handleOps tx hash, and a one-line phase-complete note (all of MAIN-01/02/03 satisfied, MAINNET_RESULT.md is the single source of truth for Phase 6).
</output>
