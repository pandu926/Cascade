---
phase: 05-mainnet-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [MAINNET_RESULT.md]
autonomous: false
requirements: [MAIN-01]
user_setup: []

must_haves:
  truths:
    - "Cascade is deployed to Pharos mainnet (chainId 1672) at a recorded address with non-empty bytecode"
    - "Cascade source is verified on the explorer (green/readable) OR verification is honestly recorded as best-effort retryable without redeploy"
    - "The deploy spend was pre-flight gated (balance read, chainId==1672 confirmed, cost estimated) before broadcast"
    - "Deployed address, deploy tx hash, before/after deployer balance, and verification status are recorded in MAINNET_RESULT.md"
  artifacts:
    - path: "MAINNET_RESULT.md"
      provides: "Single source of truth: Cascade mainnet address, deploy tx hash, explorer links, verification status"
      contains: "Cascade"
  key_links:
    - from: "deployed Cascade address"
      to: "pharosscan explorer"
      via: "verified source submission to SocialScan command_api"
      pattern: "api.socialscan.io/pharos-mainnet"
---

<objective>
Deploy the hardened `Cascade` contract to Pharos mainnet (chainId 1672) with real PROS and verify its source on the explorer. This is the headline artifact for judging: a source-verified Cascade on mainnet, recorded with an explorer link.

Purpose: Satisfies MAIN-01 and establishes the deployed address that MAIN-02 (royalty demo) and MAIN-03 (4337 demo) both reuse — no redeploy downstream.
Output: A deployed + verified Cascade, with address/tx/verification status written to a new `MAINNET_RESULT.md` (the committed single source of truth).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mainnet-deployment/05-CONTEXT.md
@.planning/phases/05-mainnet-deployment/05-RESEARCH.md

<interfaces>
<!-- Deploy target — no constructor args, single self-contained file, no imports -->
From src/Cascade.sol:
  contract Cascade  // pragma ^0.8.24, solc pinned 0.8.24, optimizer runs 200 (foundry.toml)
  mapping(address => uint256) public balances;
  uint256 public skillCount;            // public getter, returns 0 on a fresh deploy
  function register(uint256 price, uint256[] depIds, uint256[] depShares) external returns (uint256 id);
  function invoke(uint256 skillId) external payable;
  function claim() external returns (uint256 amount);

From foundry.toml:
  [rpc_endpoints] mainnet = "https://rpc.pharos.xyz"   // use --rpc-url mainnet everywhere
  solc = "0.8.24", optimizer = true, optimizer_runs = 200
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight gate + deploy Cascade to mainnet, record address</name>
  <files>MAINNET_RESULT.md</files>
  <read_first>
    - src/Cascade.sol (the no-arg contract being deployed)
    - foundry.toml (rpc_endpoints.mainnet, solc 0.8.24, optimizer 200)
    - .planning/phases/05-mainnet-deployment/05-CONTEXT.md (funding/wallet locked decisions; cast/forge create preferred over forge script which ignored --gas-price; NEVER print PRIVATE_KEY)
    - assets/networks.json (mainnet: chainId 1672, explorer https://www.pharosscan.xyz/)
  </read_first>
  <action>
    PRE-FLIGHT GATE (run FIRST, before any broadcast — STOP CLEANLY on shortfall, never half-spend):
    1. Confirm the chain: `cast chain-id --rpc-url mainnet` — MUST print `1672`. If it is anything else, STOP, record the mismatch in MAINNET_RESULT.md, and do not broadcast.
    2. Read deployer balance: `cast balance 0x3306E846b5Dc7F890436955999CeE27a6abbCbe8 --rpc-url mainnet`. Record it as the "before" balance.
    3. Estimate cost: gas price on Pharos mainnet is ~10 gwei; a no-arg Cascade CREATE is well under ~1.5M gas, so expect well under ~0.015 PROS. If balance is below a safe margin (e.g. < 0.05 PROS), STOP and record.

    NORMALIZE THE KEY WITHOUT PRINTING IT: source the gitignored .env, then set a shell var `PK=0x${PRIVATE_KEY#0x}` (strips any existing 0x, re-adds one — handles the no-prefix .env case). NEVER echo `$PK` or `$PRIVATE_KEY` to stdout or to any file.

    DEPLOY (explicit broadcast, not forge script — per CONTEXT the prior phase found `forge script --broadcast` ignored --gas-price):
    Run `forge create src/Cascade.sol:Cascade --rpc-url mainnet --private-key "$PK" --broadcast --legacy --gas-price 10000000000`. (10000000000 wei = 10 gwei, the confirmed floor; budget is ample per "gak usah ngirit" so do not undercut it.) Capture the printed `Deployed to:` address and `Transaction hash:`.
    Fallback if `forge create` misbehaves: `cast send --rpc-url mainnet --private-key "$PK" --legacy --gas-price 10000000000 --create $(forge inspect src/Cascade.sol:Cascade bytecode)` and read the contract address from the receipt.

    RECORD to a new MAINNET_RESULT.md: deployer EOA, before balance, deployed Cascade address, deploy tx hash, after balance (re-read `cast balance`), gas used (from `cast receipt`), and explorer links `https://www.pharosscan.xyz/address/<ADDR>` and `https://www.pharosscan.xyz/tx/<TXHASH>`. Mark verification status as "pending (Task 2)".
  </action>
  <acceptance_criteria>
    - `cast chain-id --rpc-url mainnet` returned exactly `1672` and this was confirmed before broadcast.
    - `cast code <ADDR> --rpc-url mainnet` returns a value longer than `0x` (deployed bytecode present).
    - `cast call <ADDR> "skillCount()(uint256)" --rpc-url mainnet` returns `0` (fresh registry, contract is live and callable).
    - `cast receipt <TXHASH> --rpc-url mainnet` shows status `1` (0x1).
    - MAINNET_RESULT.md exists and contains the deployed address, deploy tx hash, before/after deployer balance, and both explorer links.
    - No private key value appears anywhere in stdout, logs, or MAINNET_RESULT.md.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Verify Cascade source on the explorer (ranked commands), record status</name>
  <files>MAINNET_RESULT.md</files>
  <read_first>
    - .planning/phases/05-mainnet-deployment/05-RESEARCH.md (AUTHORITATIVE ranked verify commands RANK 1–6 + pitfalls; verifier URL https://api.socialscan.io/pharos-mainnet/v1/explorer/command_api/contract)
    - src/Cascade.sol (no imports, no constructor args — flatten is trivial, omit --constructor-args)
    - foundry.toml (compiler 0.8.24, optimizer runs 200 — must match the deployed bytecode)
    - MAINNET_RESULT.md (the address recorded in Task 1)
  </read_first>
  <action>
    Verification spends NO PROS (submit-only, public source) — but it must NOT block MAIN-02/03. Try the ranked commands from 05-RESEARCH.md IN ORDER, stopping at the first success:

    0. Sanity-probe the endpoint exists: `curl -s -o /dev/null -w '%{http_code}' https://api.socialscan.io/pharos-mainnet/v1/explorer/command_api/contract` should return `422` (route exists, demands module/action).
    1. RANK 1 (primary): `forge verify-contract <ADDR> src/Cascade.sol:Cascade --chain-id 1672 --compiler-version 0.8.24 --num-of-optimizations 200 --verifier blockscout --verifier-url https://api.socialscan.io/pharos-mainnet/v1/explorer/command_api/contract --watch` (NO --constructor-args; Cascade is no-arg).
    2. RANK 2 (if it demands an API key): re-run RANK 1 adding `--etherscan-api-key "verifyContract"` (dummy; Blockscout backends ignore the value).
    3. RANK 3 (if forge rejects the URL suffix): re-run with the verifier URL quoted and a trailing `?`.
    4. RANK 4 (if a full commit version is demanded): re-run with `--compiler-version v0.8.24+commit.e11b9ed9`; confirm the exact commit first from build metadata (`out/Cascade.sol/Cascade.json` → metadata.compiler.version) rather than trusting the assumed hash.
    5. RANK 5 (manual fallback): `forge flatten src/Cascade.sol > Cascade.flat.sol`, then note in MAINNET_RESULT.md that the flattened file is ready for the pharosscan UI "Verify & Publish" (Solidity single file, v0.8.24, optimizer Yes runs 200, MIT, blank constructor args). The Vercel security challenge may require an interactive browser.
    6. RANK 6 (last resort): if every path fails, record verification as BEST-EFFORT / RETRYABLE — the contract is deployed + functional (MAIN-01 partially met), verification re-attemptable later WITHOUT redeploy. Be honest in MAINNET_RESULT.md per CONTEXT: state plainly which rank was reached.

    RECORD to MAINNET_RESULT.md: which rank succeeded (or that it fell to best-effort), the verify command used, and (if verified) the explorer Contract-tab link showing green/readable source.
  </action>
  <acceptance_criteria>
    - The ranked commands were attempted in order; the first-successful rank (or the best-effort fallback) is recorded in MAINNET_RESULT.md with the exact command used.
    - On success: `forge verify-contract ... --watch` printed `Contract successfully verified`, and the explorer Contract tab at `https://www.pharosscan.xyz/address/<ADDR>` shows verified source. This link is recorded.
    - On fallback (RANK 5/6): MAINNET_RESULT.md states plainly that verification is best-effort/retryable and that re-verification needs no redeploy — no false "verified" claim.
    - Verification did not block or alter the deployed contract; the Task 1 address is unchanged.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → mainnet RPC | real-money broadcast crosses here; wrong chain or leaked key is irreversible |
| local source → SocialScan verify API | public source/bytecode submitted; no secrets cross |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | PRIVATE_KEY in .env | mitigate | Normalize key into a shell var without echoing; never write key to MAINNET_RESULT.md or stdout |
| T-05-02 | Tampering/misconfig | wrong chain target | mitigate | `cast chain-id` MUST equal 1672 confirmed before broadcast; STOP cleanly otherwise |
| T-05-03 | Denial of Service | half-spend / overspend | mitigate | Pre-flight balance read + cost estimate; STOP cleanly on shortfall |
| T-05-04 | Spoofing | verify endpoint trust | accept | api.socialscan.io is the official Pharos explorer backend (docs-cited, live-probed); verify sends only public source |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | N/A — no package installs this phase; forge/cast pre-installed |
</threat_model>

<verification>
- `cast chain-id --rpc-url mainnet` == 1672 (confirmed pre-broadcast).
- `cast code <ADDR> --rpc-url mainnet` non-empty; `cast call <ADDR> "skillCount()(uint256)"` == 0.
- `cast receipt <TXHASH> --rpc-url mainnet` status == 1.
- MAINNET_RESULT.md records address, tx hash, before/after balance, explorer links, and verification rank/status.
</verification>

<success_criteria>
Cascade is live on Pharos mainnet at a recorded address with non-empty bytecode and a callable `skillCount()`; source verification is either confirmed green on the explorer or honestly recorded as best-effort retryable; the deploy was pre-flight gated and fully recorded in MAINNET_RESULT.md. (Phase success criterion 1 + criterion 4 for this spend.)
</success_criteria>

<output>
Create `.planning/phases/05-mainnet-deployment/05-01-SUMMARY.md` when done, including the deployed Cascade mainnet address (downstream plans 05-02 and 05-03 reuse it via the `CASCADE` env var).
</output>
