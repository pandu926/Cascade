---
phase: 05-mainnet-deployment
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified: [MAINNET_RESULT.md]
autonomous: false
requirements: [MAIN-02]
user_setup: []

must_haves:
  truths:
    - "Three distinct creator EOAs each register a skill (A leaf, B dep-A, C dep-B) on the SAME mainnet Cascade from 05-01"
    - "One payer invoke of C in a single tx raises all three creator balances proportionally, summing exactly to PRICE_C"
    - "Each creator EOA was funded a small gas stipend from the deployer, with funding tx hashes recorded"
    - "Every register/invoke/funding tx hash, before/after balances, the 3 RoyaltyAccrued amounts, and explorer links are recorded in MAINNET_RESULT.md"
  artifacts:
    - path: "MAINNET_RESULT.md"
      provides: "Live royalty demo record: 3 creators, tx hashes, balance deltas, RoyaltyAccrued event amounts, explorer links"
      contains: "RoyaltyAccrued"
  key_links:
    - from: "payer invoke(idC) on mainnet Cascade"
      to: "three creator balances"
      via: "recursive _distribute fan-out crediting balances"
      pattern: "cascade.invoke"
---

<objective>
Run the recursive-royalty killer demo LIVE on Pharos mainnet: three distinct creator EOAs register an A→B→C dependency tree on the same Cascade deployed in 05-01, then one payer `invoke` of C fans real PROS up the tree so three balances rise proportionally in a single transaction.

Purpose: Satisfies MAIN-02 — the real mainnet tx where one invoke pays three creators, linkable on pharosscan. This is the core value of the project, proven with real money.
Output: All register/invoke/funding tx hashes, before/after balances, the three RoyaltyAccrued amounts (Σ == PRICE_C), and explorer links appended to MAINNET_RESULT.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mainnet-deployment/05-CONTEXT.md
@.planning/phases/05-mainnet-deployment/05-01-SUMMARY.md

<interfaces>
<!-- DemoTree.s.sol ALREADY supports this exact flow via env overrides — reuse, don't rewrite -->
From script/DemoTree.s.sol:
  Reads CREATOR_A_KEY / CREATOR_B_KEY / CREATOR_C_KEY / PRIVATE_KEY (payer) via vm.envOr.
  Reads CASCADE env (address) — if set, reuses that deployed Cascade (NO redeploy); else deploys fresh.
  PRICE_C = 0.001 ether, SHARE_C_TO_B = 5000 (50%), SHARE_B_TO_A = 4000 (40%).
  Flow: A.register(0,[],[]) -> B.register(0,[A],[4000]) -> C.register(PRICE_C,[B],[5000]) -> payer invoke{value:PRICE_C}(idC).
  Logs before/after balances + deltas + total for creatorA/B/C.

From src/Cascade.sol:
  function register(uint256 price, uint256[] depIds, uint256[] depShares) external returns (uint256 id);  // creator == msg.sender
  function invoke(uint256 skillId) external payable;   // requires msg.value == price exactly
  mapping(address => uint256) public balances;          // public getter
  event RoyaltyAccrued(uint256 indexed skillId, address indexed creator, uint256 amount);  // one per credited level

From foundry.toml:
  [rpc_endpoints] mainnet = "https://rpc.pharos.xyz"   // chainId 1672
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive + fund three creator EOAs (pre-flight gated)</name>
  <files>MAINNET_RESULT.md</files>
  <read_first>
    - script/DemoTree.s.sol (CREATOR_A/B/C_KEY env override mechanics + the demo constants)
    - .planning/phases/05-mainnet-deployment/05-CONTEXT.md (3 distinct creators needed because creator==msg.sender at register; ~0.02 PROS stipend each; record funding txs; NEVER print keys)
    - .planning/phases/05-mainnet-deployment/05-01-SUMMARY.md (the deployed Cascade address to reuse)
  </read_first>
  <action>
    Derive THREE deterministic creator keys + the payer key (the deployer). Use a demo mnemonic for the three creators so they are reproducible and recordable: e.g. `cast wallet new-mnemonic` once, OR derive with `cast wallet derive-private-key "<DEMO_MNEMONIC>" <idx>` for idx 0,1,2. Store them in shell vars CREATOR_A_KEY / CREATOR_B_KEY / CREATOR_C_KEY (NEVER printed). Compute each address with `cast wallet address --private-key "$CREATOR_X_KEY"`. The payer is the deployer: normalize as `PK=0x${PRIVATE_KEY#0x}` (never echoed).

    PRE-FLIGHT GATE (run FIRST — STOP CLEANLY on shortfall):
    1. `cast chain-id --rpc-url mainnet` MUST equal `1672`. Else STOP + record.
    2. Read deployer balance `cast balance 0x3306E846b5Dc7F890436955999CeE27a6abbCbe8 --rpc-url mainnet`; record as "before".
    3. Estimate: 3 stipends × ~0.02 PROS + 3 register-tx gas + 1 invoke gas + PRICE_C (0.001) ≈ well under 0.1 PROS at 10 gwei. If balance < a safe margin (e.g. < 0.1 PROS), STOP + record.

    FUND each creator from the deployer (record each funding tx hash): for X in A,B,C:
    `cast send <creatorX_addr> --value 0.02ether --rpc-url mainnet --private-key "$PK" --legacy --gas-price 10000000000`.
    After each, `cast receipt <txhash> --rpc-url mainnet` must show status 1; record hash + explorer link.

    RECORD to MAINNET_RESULT.md (new "MAIN-02 Royalty Demo" section): the demo mnemonic source note, the three creator addresses, each funding tx hash + explorer link, and creator balances after funding (`cast balance` each). Do NOT write any private key or the raw mnemonic secret.
  </action>
  <acceptance_criteria>
    - `cast chain-id --rpc-url mainnet` == `1672`, confirmed before any funding broadcast.
    - Three distinct creator addresses derived; `cast balance <creatorX> --rpc-url mainnet` each ≈ 0.02 PROS (non-zero) after funding.
    - Three funding `cast receipt` statuses == 1; all three tx hashes + explorer links recorded in MAINNET_RESULT.md.
    - No private key or raw mnemonic appears in stdout, logs, or MAINNET_RESULT.md.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Run live A→B→C register + single invoke on the mainnet Cascade, record deltas</name>
  <files>MAINNET_RESULT.md</files>
  <read_first>
    - script/DemoTree.s.sol (the driver: reads CASCADE env to reuse the deployed contract; A/B/C register + payer invoke; logs deltas)
    - src/Cascade.sol (register requires creator==msg.sender; invoke requires msg.value==price; RoyaltyAccrued emitted per level)
    - .planning/phases/05-mainnet-deployment/05-CONTEXT.md (reuse SAME Cascade from MAIN-01, NO redeploy; record every tx + before/after + RoyaltyAccrued amounts; Phase 6 needs event-level detail)
  </read_first>
  <action>
    PRE-BROADCAST GUARD (run FIRST — STOP CLEANLY before ANY spend; this is a distinct broadcast batch from Task 1's funding, and DemoTree.s.sol has a silent `if (existing == address(0)) new Cascade()` fallback + NO chainid guard, so this guard is what prevents an unintended real-money fresh redeploy):
    1. `cast chain-id --rpc-url mainnet` MUST equal `1672`. Else STOP + record (never broadcast to the wrong chain).
    2. Set `CASCADE=<deployed_address_from_05-01-SUMMARY.md>` and ASSERT it is non-empty AND `cast code $CASCADE --rpc-url mainnet` returns non-empty bytecode (length > 2, i.e. not just `0x`). If CASCADE is unset/empty OR has no code, STOP CLEANLY + record — do NOT run DemoTree (this is the only thing standing between us and its fresh-deploy branch spending real PROS on the wrong contract).
    3. Re-read deployer balance; confirm ≥ a safe margin for register×3 + invoke + PRICE_C. STOP on shortfall.

    Then export `CASCADE` (verified above) so DemoTree.s.sol reuses it (NO redeploy). Export CREATOR_A_KEY/B_KEY/C_KEY and PRIVATE_KEY (payer) into the script env without echoing them.

    Per CONTEXT, the prior live phase found `forge script --broadcast` ignored --gas-price and sent a failed CREATE; DemoTree mixes multiple signers (3 creators + payer) which forge script handles via vm.startBroadcast(key). Run it as a broadcast script BUT pass an explicit legacy gas price and verify each resulting tx:
    `CASCADE=<ADDR> forge script script/DemoTree.s.sol:DemoTreeScript --rpc-url mainnet --broadcast --legacy --with-gas-price 10000000000 -vvv`.
    If forge script still mis-handles gas/multi-broadcast on this node (watch for a failed/again-ignored gas price like the Phase 1 symptom), FALL BACK to discrete `cast send` calls reproducing the exact DemoTree flow, signing each register with the matching creator key and the invoke with the payer key:
      - A: `cast send <CASCADE> "register(uint256,uint256[],uint256[])(uint256)" 0 "[]" "[]" --private-key "$CREATOR_A_KEY" --rpc-url mainnet --legacy --gas-price 10000000000` → idA
      - B: `register 0 "[idA]" "[4000]"` signed CREATOR_B_KEY → idB
      - C: `register 1000000000000000 "[idB]" "[5000]"` signed CREATOR_C_KEY → idC  (1e15 wei = 0.001 PROS)
      - payer: `cast send <CASCADE> "invoke(uint256)" idC --value 0.001ether --private-key "$PK" --rpc-url mainnet --legacy --gas-price 10000000000`

    Capture balances BEFORE and AFTER the invoke via `cast call <CASCADE> "balances(address)(uint256)" <creatorX> --rpc-url mainnet` for A/B/C. Decode the three RoyaltyAccrued event amounts from the invoke receipt (`cast receipt <invoke_tx> --rpc-url mainnet` / `cast logs`).

    RECORD to MAINNET_RESULT.md: skill ids A/B/C, each register tx hash, the invoke tx hash, before/after balances per creator, the three deltas, the three RoyaltyAccrued amounts, that Σ(deltas) == 0.001 PROS exactly, and explorer links for every tx.
  </action>
  <acceptance_criteria>
    - PRE-BROADCAST GUARD passed: `cast chain-id --rpc-url mainnet` == 1672 AND `cast code $CASCADE --rpc-url mainnet` returned non-empty bytecode (>2 chars) BEFORE any DemoTree/cast broadcast — proving the fresh-deploy fallback can never fire.
    - The mainnet Cascade from 05-01 was reused (CASCADE env set); `cast call <CASCADE> "skillCount()(uint256)" --rpc-url mainnet` increased by exactly 3 (A,B,C) — no redeploy.
    - The invoke `cast receipt` status == 1, and `msg.value` was exactly 0.001 PROS (invoke would revert WrongValue otherwise).
    - All three creator balances rose: `cast call balances(address)` after > before for creatorA, creatorB, creatorC.
    - The three balance deltas sum to exactly 1000000000000000 wei (0.001 PROS) — conservation holds.
    - MAINNET_RESULT.md records all register tx hashes, the invoke tx hash, before/after balances, the three deltas, the three RoyaltyAccrued amounts, and explorer links for each tx.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local shell → mainnet RPC | real-money funding + invoke broadcasts cross here; irreversible |
| deployer EOA → 3 creator EOAs | gas stipends move real PROS to derived keys |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05 | Information Disclosure | creator keys + mnemonic + PRIVATE_KEY | mitigate | Keep in shell vars only; never echo; never write to MAINNET_RESULT.md |
| T-05-06 | Tampering/misconfig | wrong chain or redeploy | mitigate | chain-id==1672 confirmed pre-broadcast; CASCADE env forces reuse, skillCount delta == 3 proves no redeploy |
| T-05-07 | Denial of Service | half-spend across multi-tx demo | mitigate | Pre-flight estimate covers stipends + register + invoke + PRICE_C; STOP cleanly on shortfall |
| T-05-08 | Tampering | wrong invoke value | accept | Cascade enforces msg.value==price on-chain (WrongValue revert) — a wrong value fails safe, no partial pay |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | N/A — no package installs this phase |
</threat_model>

<verification>
- `cast chain-id --rpc-url mainnet` == 1672 (pre-broadcast).
- `cast call <CASCADE> "skillCount()(uint256)"` increased by exactly 3.
- invoke `cast receipt` status == 1; three creator balances rose; Σ deltas == 1e15 wei.
- MAINNET_RESULT.md records every tx hash, before/after balances, RoyaltyAccrued amounts, explorer links.
</verification>

<success_criteria>
A single live mainnet invoke of an A→B→C tree raised three distinct creator balances proportionally in one tx, summing exactly to PRICE_C; every funding/register/invoke tx is pre-flight gated and recorded with explorer links in MAINNET_RESULT.md. (Phase success criterion 2 + criterion 4 for this spend.)
</success_criteria>

<output>
Create `.planning/phases/05-mainnet-deployment/05-02-SUMMARY.md` when done, including the registered skill ids (A/B/C) on the mainnet Cascade — 05-03 may reuse these skills for the batched UserOp.
</output>
