---
phase: 06-visualization-docs-submission
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - README.md
  - docs/DEMO_VIDEO_SCRIPT.md
  - SUBMISSION.md
autonomous: true
requirements: [DEMO-04, DEMO-05, DEMO-06]
must_haves:
  truths:
    - "A fresh reader can install Foundry, run forge test, configure PRIVATE_KEY, and run the demo from the README quickstart"
    - "The README documents the verified mainnet addresses with pharosscan links and is honest about what is/isn't proven (live+verified+fuzz-tested, NOT an independent audit)"
    - "A recordable shot-by-shot video script exists (<= ~2-3 min) covering the WOW viz, one invoke fanning to three creators, and the 4337 batch"
    - "A DoraHacks submission checklist exists with all form fields pre-filled, the public-repo push commands, and the carried-over pharosscan Verified visual-check line"
  artifacts:
    - path: "README.md"
      provides: "Top-level project README: what/why, architecture, quickstart, verified mainnet addresses, hackathon mapping, doc links"
      contains: "## Quickstart"
      min_lines: 60
    - path: "docs/DEMO_VIDEO_SCRIPT.md"
      provides: "Shot-by-shot storyboard + narration, <= ~2-3 min, marked awaiting human recording"
    - path: "SUBMISSION.md"
      provides: "DoraHacks submission checklist with pre-filled form fields + push commands + verify-checkmark line"
  key_links:
    - from: "README.md"
      to: "https://www.pharosscan.xyz/address/0x31bE4C6B5711913D818e377ebd809d4397FF3c84"
      via: "verified mainnet address link"
      pattern: "pharosscan\\.xyz/address/0x31bE4C6B"
    - from: "README.md"
      to: "SKILL.md, SECURITY.md, MAINNET_RESULT.md, web/index.html"
      via: "relative doc links"
      pattern: "SECURITY\\.md|MAINNET_RESULT\\.md|web/index\\.html"
---

<objective>
Build all documentation + submission materials: a polished top-level `README.md` (DEMO-04), a recordable `docs/DEMO_VIDEO_SCRIPT.md` (DEMO-05 materials), and a `SUBMISSION.md` DoraHacks checklist with pre-filled form fields (DEMO-06 materials). The README replaces the current default Foundry boilerplate.

Purpose: Give a fresh reader and the judges everything they need — set up + run the repo, see the verified mainnet proof, understand the killer idea, and (for the human) a turnkey path to record + submit. Recording the video and the actual submission are human actions completed in Plan 03; this plan produces every artifact they depend on.
Output: `README.md`, `docs/DEMO_VIDEO_SCRIPT.md`, `SUBMISSION.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-visualization-docs-submission/06-CONTEXT.md
@MAINNET_RESULT.md
@SKILL.md
@SECURITY.md
@assets/networks.json

<facts_for_docs>
<!-- All verbatim from the repo / MAINNET_RESULT.md. -->
- Project: Cascade — Pharos recursive skill royalties. Analogy: npm, but every package earns a cut every time it's used downstream — automatically, instantly, recursively.
- Track: Pharos Skill-to-Agent Dual Cascade Hackathon — Skill Hackathon track (20,000 PROS, 40 winners).
- Architecture: Cascade.sol (recursive royalty registry + payment router, pull-payment) + ERC-4337 AA layer (src/aa/* — CascadeAccount + AccountFactory, EntryPoint v0.7 self-bundled handleOps) + Anthropic Skill packaging (SKILL.md + references/ + assets/networks.json).
- Test suite: forge test — 41 green (incl. fuzz + stateful invariant), committed .gas-snapshot, slither-clean. Honesty bar: live + source-verified on mainnet, fuzz/invariant-tested, slither-clean — NOT an independent professional audit (see SECURITY.md caveat).
- Quickstart: install Foundry (foundryup) -> forge test (41 pass) -> cp .env.example .env + set PRIVATE_KEY -> run register/invoke/claim per references/ against the network in assets/networks.json (testnet default, mainnet supported).
- Verified mainnet (chainId 1672, PROS, https://www.pharosscan.xyz/):
  - Cascade (verified): 0x31bE4C6B5711913D818e377ebd809d4397FF3c84
  - Royalty invoke tx (one invoke pays 3 creators, Sigma 0.001 PROS): 0x5ba20c8771787ff4bc4ea5c938fa32a394f4c1103318b7cfe0039f19dd3b1564
  - 4337 handleOps tx (one UserOp batches 2 invokes via real EntryPoint v0.7): 0x1f3cec937acec167db716adf10be50bf135ac08f9ba3f02974cc0ee524375f90
  - EntryPoint v0.7: 0x0000000071727De22E5E9d8BAf0edAc6f37da032; Factory: 0x904935BA1417FC35591019A0fC54c670DA824c60; Account: 0xfe93754C8730f13257e9d733dDd7c9037f2e1Ef1
- Testnet history (Phase 1, also real) can be mentioned as the earlier proof.
- Repo is greenfield git with NO remote yet (DEMO-06 push needed). Deadline: 2026-06-15 15:59.
- The WOW viz lives at web/index.html (built in Plan 01) — README + script + submission link to it.
</facts_for_docs>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Top-level README (DEMO-04) — what/why, architecture, quickstart, verified mainnet, links</name>
  <files>README.md</files>
  <read_first>
    - README.md (current default Foundry boilerplate to fully replace)
    - .planning/PROJECT.md + MAINNET_RESULT.md + SKILL.md + SECURITY.md (source of every claim/address/link)
    - .planning/phases/06-visualization-docs-submission/06-CONTEXT.md (DEMO-04 spec + honesty bar)
    - assets/networks.json (network names/chainIds/explorer URLs)
  </read_first>
  <action>
    Replace the default Foundry README.md entirely with a real project README. Sections (use `##` headers): a one-liner + the npm analogy; "The killer idea" (one invoke pays every creator up the dependency tree, proportional to depth, trustless, single tx, pull-payment); "Architecture" (Cascade.sol recursive router + ERC-4337 AA layer in src/aa/* + Anthropic Skill packaging); "## Quickstart" (install Foundry via foundryup; `forge test` -> 41 green incl. fuzz/invariant; `cp .env.example .env` + set PRIVATE_KEY; run register/invoke/claim per references/ against the network from assets/networks.json — testnet default, mainnet supported; and "open web/index.html" to see the visualization); "Live on Pharos mainnet" (a table of the VERIFIED addresses + both demo tx hashes, each a clickable https://www.pharosscan.xyz/ link; note the earlier testnet proof from Phase 1); "Hackathon" (Skill Hackathon track mapping); "Docs" (relative links to SKILL.md, references/, SECURITY.md, MAINNET_RESULT.md, and web/index.html). Add a short honesty note: live + source-verified on mainnet, fuzz/invariant-tested, slither-clean — NOT an independent professional audit (link SECURITY.md). Use only values from <facts_for_docs>. Keep it scannable and accurate; no invented metrics.
  </action>
  <verify>
    <automated>grep -q "## Quickstart" README.md && grep -q "0x31bE4C6B5711913D818e377ebd809d4397FF3c84" README.md && grep -q "pharosscan.xyz/address/0x31bE4C6B" README.md && grep -q "0x5ba20c8771787ff4bc4ea5c938fa32a394f4c1103318b7cfe0039f19dd3b1564" README.md && grep -q "0x1f3cec937acec167db716adf10be50bf135ac08f9ba3f02974cc0ee524375f90" README.md && grep -Eq "SECURITY\.md" README.md && grep -Eq "MAINNET_RESULT\.md" README.md && grep -Eq "web/index\.html" README.md && grep -qi "forge test" README.md && test $(wc -l < README.md) -ge 60</automated>
  </verify>
  <acceptance_criteria>
    - README.md contains a `## Quickstart` with foundryup + `forge test` + PRIVATE_KEY setup + run-the-demo + open web/index.html steps.
    - It lists the verified Cascade address and BOTH demo tx hashes, each linked to www.pharosscan.xyz.
    - It links SKILL.md, SECURITY.md, MAINNET_RESULT.md, references/, and web/index.html (relative links).
    - It states the honest scope (verified+fuzz-tested+slither-clean, NOT an independent audit) and links SECURITY.md.
    - The default Foundry boilerplate is gone; no invented numbers (only values from MAINNET_RESULT.md).
  </acceptance_criteria>
  <done>README.md is a real, honest, runnable project entry point pointing at the verified mainnet proof and the visualization.</done>
</task>

<task type="auto">
  <name>Task 2: Demo video script (DEMO-05 materials) + DoraHacks submission checklist (DEMO-06 materials)</name>
  <files>docs/DEMO_VIDEO_SCRIPT.md, SUBMISSION.md</files>
  <read_first>
    - MAINNET_RESULT.md (the real txs/addresses the script narrates and the submission links)
    - .planning/phases/06-visualization-docs-submission/06-CONTEXT.md (DEMO-05 script spec; DEMO-06 form fields + the pharosscan Verified visual-check residual)
    - SECURITY.md (the verification residual + honesty caveat the submission must carry)
    - README.md (Task 1 — the submission references it; keep links consistent)
  </read_first>
  <action>
    Create `docs/DEMO_VIDEO_SCRIPT.md`: a shot-by-shot storyboard + narration, total runtime <= ~2-3 minutes, as a numbered shot list (each shot: visual / on-screen action / narration line / approx duration). Arc: (1) open on the WOW viz (web/index.html) — state the npm-but-recursive-royalties idea; (2) press play — ONE invoke sends the payment particle up the A->B->C tree, three balances rise to A 0.0002 / B 0.0003 / C 0.0005, Sigma 0.001 PROS; (3) cut to the real pharosscan invoke tx (0x5ba20c87…) open in a browser to prove it ran on mainnet; (4) the 4337 batch — one self-bundled handleOps (0x1f3cec93…) pays two skills via the real EntryPoint v0.7; (5) close on the verified Cascade address. Add a header note: "Awaiting human recording — user opted to skip recording in this phase." Do NOT produce a video file.
    Create `SUBMISSION.md`: a DoraHacks submission checklist. Include (a) a "Push to a public remote" section with ready-to-run git commands (create repo on GitHub/GitLab, `git remote add origin <URL>`, `git push -u origin main`) — note the repo currently has NO remote; (b) "DoraHacks form fields (pre-filled)": Skill/Project name = Cascade; short description (recursive skill royalties on Pharos, one invoke pays the whole dependency tree); GitHub link (placeholder for the pushed URL); demo link (the web viz / uploaded video); setup instructions (point to README Quickstart); supported framework = Anthropic Agent Skills + Foundry; dependencies/notes (Foundry CLI; verified mainnet contract); track = Skill Hackathon; (c) "Record + upload video" referencing docs/DEMO_VIDEO_SCRIPT.md; (d) a checklist line: "Visually confirm the pharosscan 'Verified' green checkmark on the Cascade Code tab (https://www.pharosscan.xyz/address/0x31bE4C6B5711913D818e377ebd809d4397FF3c84) — read API cannot confirm programmatically" (the carried-over Phase 5 residual); (e) "Submit before deadline 2026-06-15 15:59". Make every item a `- [ ]` checkbox so the human can tick through it.
  </action>
  <verify>
    <automated>test -f docs/DEMO_VIDEO_SCRIPT.md && grep -Eqi "shot|scene" docs/DEMO_VIDEO_SCRIPT.md && grep -q "0x5ba20c8771787ff4bc4ea5c938fa32a394f4c1103318b7cfe0039f19dd3b1564" docs/DEMO_VIDEO_SCRIPT.md && grep -Eqi "awaiting human recording|skip" docs/DEMO_VIDEO_SCRIPT.md && test -f SUBMISSION.md && grep -qi "git push" SUBMISSION.md && grep -qi "DoraHacks" SUBMISSION.md && grep -qi "Anthropic Agent Skills" SUBMISSION.md && grep -qi "Verified" SUBMISSION.md && grep -q "0x31bE4C6B5711913D818e377ebd809d4397FF3c84" SUBMISSION.md && grep -q "2026-06-15" SUBMISSION.md && grep -q "\- \[ \]" SUBMISSION.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/DEMO_VIDEO_SCRIPT.md is a numbered shot list (<= ~2-3 min) covering the viz + one-invoke-three-creators + the 4337 batch, references the real invoke tx, and is marked "awaiting human recording".
    - SUBMISSION.md has the public-repo push commands (noting no remote yet), DoraHacks form fields pre-filled (name Cascade, description, GitHub/demo links, instructions, framework = Anthropic Agent Skills + Foundry, dependencies, track), the record+upload-video step, the pharosscan Verified-checkmark visual-check line, and the 2026-06-15 15:59 deadline.
    - Every actionable item in SUBMISSION.md is a `- [ ]` checkbox.
  </acceptance_criteria>
  <done>The video script and submission checklist are complete and turnkey; the human can record and submit by following them.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo -> public remote | DEMO-06 push exposes the repo publicly (human action, gated in Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Information disclosure | public push | mitigate | SUBMISSION.md reminds the human to confirm `.env` / private keys are gitignored before pushing; .gitignore already excludes .env |
| T-06-05 | Repudiation | submission claims | mitigate | docs claim only what MAINNET_RESULT.md proves; honesty caveat (NOT an independent audit) carried from SECURITY.md |
</threat_model>

<verification>
- README quickstart steps are runnable (Foundry install, `forge test`, env setup, run demo, open viz) and all addresses/links match MAINNET_RESULT.md.
- docs/DEMO_VIDEO_SCRIPT.md and SUBMISSION.md exist with the required sections; no video file produced.
- SUBMISSION.md carries the pharosscan Verified visual-check line and the deadline.
</verification>

<success_criteria>
A fresh reader can set up and run the repo from the README and see the verified mainnet proof; a complete recordable video script and a turnkey DoraHacks submission checklist (with the carried-over verify-checkmark residual) exist for the human to execute in Plan 03.
</success_criteria>

<output>
Create `.planning/phases/06-visualization-docs-submission/06-02-SUMMARY.md` when done.
</output>
