---
phase: 06-visualization-docs-submission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web/data.json
  - web/index.html
  - web/styles.css
  - web/app.js
autonomous: true
requirements: [DEMO-03]
must_haves:
  truths:
    - "Opening web/index.html in a browser (no build step, no node_modules) renders an A->B->C dependency tree"
    - "Pressing play animates a payment particle traveling UP the tree, splitting at each node, with creator balances ticking up to A +0.0002, B +0.0003, C +0.0005, Sigma 0.001 PROS"
    - "The page shows the real verified mainnet Cascade address and both real tx hashes as clickable pharosscan links"
    - "Both demos are visible: the recursive royalty split (MAIN-02) and the ERC-4337 self-bundled batch (MAIN-03)"
    - "A replay control lets the animation be re-run for recording/judging"
  artifacts:
    - path: "web/data.json"
      provides: "Committed snapshot of the real mainnet event data (addresses, tx hashes, wei deltas)"
      contains: "0x31bE4C6B5711913D818e377ebd809d4397FF3c84"
    - path: "web/index.html"
      provides: "Self-contained static visualization entry point, no build step"
      contains: "0x5ba20c8771787ff4bc4ea5c938fa32a394f4c1103318b7cfe0039f19dd3b1564"
    - path: "web/styles.css"
      provides: "Dark on-chain-terminal design system, compositor-friendly motion"
    - path: "web/app.js"
      provides: "Tree rendering + animated money-flow engine + replay control"
  key_links:
    - from: "web/app.js"
      to: "web/data.json"
      via: "fetch('./data.json') with graceful fallback to inline snapshot"
      pattern: "data\\.json"
    - from: "web/index.html"
      to: "https://www.pharosscan.xyz"
      via: "anchor hrefs to real tx + address pages"
      pattern: "pharosscan\\.xyz"
---

<objective>
Build the headline WOW web visualization (DEMO-03): a self-contained static page under `web/` that renders the A->B->C dependency tree and animates ONE `invoke` sending a payment particle UP the tree, splitting at each node, with creator balances ticking up to their REAL mainnet values (A +0.0002, B +0.0003, C +0.0005, Sigma 0.001 PROS). It also shows the ERC-4337 self-bundled batch and surfaces the real verified-mainnet tx hashes + Cascade address as clickable pharosscan links.

Purpose: This single animation makes the project's core value — recursive royalties paid in one transaction — impossible to misread, and the clickable real tx links prove it actually ran on Pharos mainnet. This is the artifact judges remember.
Output: `web/data.json`, `web/index.html`, `web/styles.css`, `web/app.js` — opens with `open web/index.html`, no build step, no node_modules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-visualization-docs-submission/06-CONTEXT.md
@MAINNET_RESULT.md

<data_snapshot>
<!-- The ONLY source of truth for every number/address/hash the viz embeds. All from MAINNET_RESULT.md. NO mocked values. -->
Network: Pharos mainnet, chainId 1672, native token PROS, explorer https://www.pharosscan.xyz/

Cascade (verified): 0x31bE4C6B5711913D818e377ebd809d4397FF3c84

Tree (MAIN-02 royalty demo):
- A  id 1  leaf, price 0           creator 0x7ca05d52EB17833E802B7D2eC7f1Fc23950c56b8
- B  id 2  dep A @ 4000 bps (40%)  creator 0xD79F121Ac383e3e7f2aeEa6AEb3b700e2Fb6796b
- C  id 3  dep B @ 5000 bps (50%), price 1000000000000000 (0.001 PROS)  creator 0xECe4BBabd00c22E1baA1dE7f83E152D0eB6D12ef

MAIN-02 invoke tx: 0x5ba20c8771787ff4bc4ea5c938fa32a394f4c1103318b7cfe0039f19dd3b1564
Creator deltas from the single invoke (wei | PROS | share of PRICE_C):
- A id1: 200000000000000 | 0.0002 | 20%
- B id2: 300000000000000 | 0.0003 | 30%
- C id3: 500000000000000 | 0.0005 | 50%
- Sigma: 1000000000000000 | 0.001 | 100%  (conservation holds exactly == PRICE_C)
Split math: C keeps 50% (0.0005); 50% routes to B subtree -> B keeps 60% of that (0.0003); 40% routes to A leaf (0.0002).

MAIN-03 4337 demo:
- handleOps tx: 0x1f3cec937acec167db716adf10be50bf135ac08f9ba3f02974cc0ee524375f90
- EntryPoint v0.7: 0x0000000071727De22E5E9d8BAf0edAc6f37da032
- AccountFactory: 0x904935BA1417FC35591019A0fC54c670DA824c60
- CascadeAccount (smart account, payer): 0xfe93754C8730f13257e9d733dDd7c9037f2e1Ef1
- ONE UserOp batched TWO invokes: executeBatch([cascade,cascade],[0,1e15],[invoke(2),invoke(3)]) -> 2 Invoked + 3 RoyaltyAccrued events
- cumulative creator balances after the batch: A 0.0004, B 0.0006, C 0.001 PROS
</data_snapshot>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Real-data snapshot + page shell + dark on-chain design system</name>
  <files>web/data.json, web/index.html, web/styles.css</files>
  <read_first>
    - MAINNET_RESULT.md (the ONLY source of every value — addresses, tx hashes, wei deltas, tree shape)
    - .planning/phases/06-visualization-docs-submission/06-CONTEXT.md (DEMO-03 WOW direction + anti-template design bar)
  </read_first>
  <action>
    Create `web/data.json` as a committed snapshot of the real mainnet events, using ONLY the values in <data_snapshot> / MAINNET_RESULT.md. Shape it as: a `network` object (name, chainId 1672, token "PROS", explorer "https://www.pharosscan.xyz/", cascade address); a `tree` array of three nodes A/B/C each with {id, label, creator address, depBps, priceWei, deltaWei, deltaPros, sharePct}; a `royaltyDemo` object {invokeTxHash, priceWei "1000000000000000", sumWei "1000000000000000"}; a `batchDemo` object {handleOpsTxHash, entryPoint, factory, account, batchedSkillIds [2,3], cumulative {A,B,C in PROS}}. Store wei as strings (avoid JS number precision loss on 1e15). Every address/hash copied verbatim from MAINNET_RESULT.md.
    Create `web/index.html` as semantic HTML5 (header/main/section/footer), NO build step, NO framework CDN. Sections: a title header ("Cascade — recursive skill royalties, live on Pharos mainnet"); a "Royalty split" section hosting the SVG tree stage + a balances panel + a play/replay control (wired in Task 2); a "4337 batch" section (wired in Task 3); a footer with the verified Cascade address and BOTH real tx hashes as `<a target="_blank" rel="noopener">` links to `https://www.pharosscan.xyz/tx/<hash>` and `https://www.pharosscan.xyz/address/0x31bE4C6B5711913D818e377ebd809d4397FF3c84`. Include `<script src="./app.js" defer></script>`. Embed the data.json content ALSO as an inline `<script type="application/json" id="snapshot">` block so the page works when opened via file:// (where fetch of a sibling file is blocked by CORS) — app.js (Task 2) prefers fetch and falls back to this inline block.
    Create `web/styles.css` as a dark on-chain-terminal design system per 06-CONTEXT: CSS custom-property tokens (palette, mono + display type scale with clamp(), spacing, durations, easing), monospace for data/hashes, a real node-graph layout (NOT a card grid), clear scale-contrast hierarchy, designed hover/focus states on links and the play control, and subtle atmosphere (grain/glow gradient) fitting the theme. Motion tokens must target ONLY transform/opacity. No hardcoded hex repeated inline — use tokens.
  </action>
  <verify>
    <automated>test -f web/data.json && test -f web/index.html && test -f web/styles.css && python3 -c "import json;d=json.load(open('web/data.json'));assert d['network']['cascade']=='0x31bE4C6B5711913D818e377ebd809d4397FF3c84';assert any(n['deltaWei']=='200000000000000' for n in d['tree']);assert any(n['deltaWei']=='300000000000000' for n in d['tree']);assert any(n['deltaWei']=='500000000000000' for n in d['tree']);assert d['royaltyDemo']['sumWei']=='1000000000000000'" && grep -q "0x5ba20c8771787ff4bc4ea5c938fa32a394f4c1103318b7cfe0039f19dd3b1564" web/index.html && grep -q "0x1f3cec937acec167db716adf10be50bf135ac08f9ba3f02974cc0ee524375f90" web/index.html && grep -q "pharosscan.xyz" web/index.html && python3 -c "import xml.dom.minidom,re;h=open('web/index.html').read();assert '<!doctype html' in h.lower() or '<html' in h.lower()"</automated>
  </verify>
  <acceptance_criteria>
    - web/data.json is valid JSON and contains the verified Cascade address `0x31bE4C6B5711913D818e377ebd809d4397FF3c84`, the three real deltas as strings (200000000000000, 300000000000000, 500000000000000), and sumWei 1000000000000000.
    - web/index.html is valid HTML, references BOTH real tx hashes (0x5ba20c87… royalty, 0x1f3cec93… handleOps) and links to www.pharosscan.xyz, and includes the inline `id="snapshot"` JSON fallback block.
    - web/styles.css defines CSS custom-property tokens and uses only transform/opacity for any transitions (no animating width/height/top/left).
    - No build step, no framework CDN, no node_modules required to open the file.
  </acceptance_criteria>
  <done>Snapshot, page shell, and design system exist; data.json carries the real mainnet values; index.html links the real txs to pharosscan.</done>
</task>

<task type="auto">
  <name>Task 2: The WOW money-flow animation — particle up the tree, balances tick up, replay</name>
  <files>web/app.js</files>
  <read_first>
    - web/data.json (the snapshot Task 1 created — drives every number)
    - web/index.html (the SVG stage + balances panel + play control element IDs to bind)
    - .planning/phases/06-visualization-docs-submission/06-CONTEXT.md (the WOW moment spec: ONE invoke, particle travels UP, splits at each node, balances tick to A+0.0002/B+0.0003/C+0.0005, Sigma 0.001)
  </read_first>
  <action>
    Create `web/app.js` (vanilla JS, no bundler). On load: read the snapshot — prefer `fetch('./data.json')`, and on any failure (file:// CORS) fall back to parsing the inline `#snapshot` JSON block. Render the A->B->C tree as SVG nodes + connecting edges into the royalty-split stage from Task 1, labeling each node with its creator address (truncated mid) and a live balance readout starting at 0.
    Animate the core sequence on play: an `invoke(C)` of 0.001 PROS spawns a payment particle that travels UP the edges (C -> B -> A), and at each node a portion "splits off" and that creator's balance counter ticks up to its real value — C +0.0005, B +0.0003, A +0.0002 — with a running Sigma reaching exactly 0.001 PROS. Drive motion with requestAnimationFrame and/or CSS transitions on transform/opacity ONLY (compositor-friendly per web rules); respect `prefers-reduced-motion` (snap to final state, no particle flight). Animate balance numbers by interpolating wei (BigInt-safe or integer math) and formatting to PROS so the displayed totals land on the exact snapshot values.
    Wire the play/replay control to reset balances to 0 and re-run the sequence (must be re-runnable for video capture). Keep the animation crisp and legible at video resolution — large type, high contrast, clear "money is flowing UP and being split" reading.
  </action>
  <verify>
    <automated>test -f web/app.js && grep -Eq "requestAnimationFrame|transition|animate" web/app.js && grep -q "snapshot" web/app.js && grep -Eq "fetch\(.?\./data\.json" web/app.js && grep -q "prefers-reduced-motion" web/app.js && node --check web/app.js</automated>
  </verify>
  <acceptance_criteria>
    - web/app.js loads the snapshot via fetch with a documented fallback to the inline `#snapshot` block (so it works from file://).
    - The animation references requestAnimationFrame and/or CSS transitions and animates only transform/opacity.
    - Balances tick to the exact snapshot values (A 0.0002, B 0.0003, C 0.0005, Sigma 0.001 PROS) — derived from data.json, not hardcoded literals duplicated in app.js.
    - A play/replay control resets and re-runs the sequence; `prefers-reduced-motion` is honored.
    - `node --check web/app.js` passes (valid JS).
  </acceptance_criteria>
  <done>Pressing play sends a payment particle up the A->B->C tree, splitting at each node, with balances ticking to the real mainnet deltas; replay re-runs it.</done>
</task>

<task type="auto">
  <name>Task 3: ERC-4337 batch view + pharosscan link polish + final legibility pass</name>
  <files>web/index.html, web/app.js, web/styles.css</files>
  <read_first>
    - web/index.html, web/app.js, web/styles.css (the artifacts from Tasks 1-2 to extend)
    - MAINNET_RESULT.md (MAIN-03 section: handleOps tx, EntryPoint, factory, account, the 2-invoke batch, cumulative balances A 0.0004 / B 0.0006 / C 0.001)
    - .planning/phases/06-visualization-docs-submission/06-CONTEXT.md (show BOTH demos; batch = one self-bundled UserOp pays two skills)
  </read_first>
  <action>
    Build the "4337 batch" section using the `batchDemo` data from data.json: render the smart account (`0xfe93754C…1Ef1`) as the single payer self-bundling ONE `handleOps` through the real EntryPoint v0.7, fanning into the SAME tree via TWO batched invokes (skill ids 2 and 3), and show the cumulative creator balances doubling/rising (A 0.0004, B 0.0006, C 0.001). Provide a section toggle or sequenced reveal so a judge sees the royalty split first, then the batch (per 06-CONTEXT). Reuse the Task 2 tree renderer/animation primitives — do not duplicate the engine.
    Surface every relevant identifier as a clickable pharosscan link (target _blank, rel noopener): the handleOps tx, the EntryPoint address, the factory, the smart account, and the verified Cascade address. Truncate hashes/addresses mid for legibility but link the full value.
    Final legibility pass: verify large type + high contrast at 1080p, designed hover/focus states on all links and controls, and that nothing depends on a network call (snapshot fallback path works offline). Keep motion on transform/opacity only.
  </action>
  <verify>
    <automated>grep -q "0x1f3cec937acec167db716adf10be50bf135ac08f9ba3f02974cc0ee524375f90" web/index.html && grep -q "0x0000000071727De22E5E9d8BAf0edAc6f37da032" web/index.html && grep -q "0xfe93754C8730f13257e9d733dDd7c9037f2e1Ef1" web/index.html && grep -q "0x904935BA1417FC35591019A0fC54c670DA824c60" web/index.html && grep -c "pharosscan.xyz/tx/\|pharosscan.xyz/address/" web/index.html && node --check web/app.js && python3 -c "import json;d=json.load(open('web/data.json'));assert d['batchDemo']['handleOpsTxHash']=='0x1f3cec937acec167db716adf10be50bf135ac08f9ba3f02974cc0ee524375f90'"</automated>
  </verify>
  <acceptance_criteria>
    - The 4337 section renders the handleOps tx, EntryPoint, factory, and smart account — all as clickable pharosscan links — and shows the batch crediting both skills (cumulative A 0.0004 / B 0.0006 / C 0.001).
    - Both demos are reachable on the page (toggle or sequence).
    - The batch view reuses the Task 2 engine (no duplicated animation logic).
    - Page opens and animates with zero network access (inline-snapshot fallback verified); `node --check web/app.js` passes.
  </acceptance_criteria>
  <done>Both mainnet demos render with real clickable pharosscan links; the page is a crisp, offline, video-ready WOW visualization.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> external links | User clicks pharosscan anchors that open third-party explorer pages |
| browser -> optional RPC | If a live-fetch fallback is added, a read-only RPC GET crosses to rpc.pharos.xyz |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | external anchor links | mitigate | all external `<a>` use `rel="noopener"` (and `target="_blank"`) to prevent reverse-tabnabbing |
| T-06-02 | Information disclosure | viz data | accept | data is already-public verified on-chain mainnet data; no secrets, no keys, no PII in web/ |
| T-06-03 | Denial of service | offline rendering | mitigate | committed snapshot + inline fallback means the page never depends on a reachable RPC/CORS endpoint |
</threat_model>

<verification>
- `open web/index.html` (or `python3 -m http.server` in web/) renders the tree, plays the money-flow animation to the exact mainnet deltas, and shows both demos with working pharosscan links — entirely offline.
- All embedded numbers/addresses/hashes match MAINNET_RESULT.md exactly (no mocked values).
- `node --check web/app.js` passes; data.json is valid JSON.
</verification>

<success_criteria>
A self-contained static page under web/ renders the A->B->C dependency tree and animates money flowing up it to the real mainnet deltas (Sigma 0.001 PROS), shows both the royalty split and the 4337 batch, surfaces real pharosscan links, and runs with no build step and no network dependency.
</success_criteria>

<output>
Create `.planning/phases/06-visualization-docs-submission/06-01-SUMMARY.md` when done.
</output>
