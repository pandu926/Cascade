---
phase: 06-visualization-docs-submission
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified: []
autonomous: false
requirements: [DEMO-05, DEMO-06]
must_haves:
  truths:
    - "The human has recorded + uploaded the demo video following docs/DEMO_VIDEO_SCRIPT.md"
    - "The human has pushed the repo to a public remote and submitted to DoraHacks using SUBMISSION.md"
    - "The human has visually confirmed the pharosscan 'Verified' green checkmark on the Cascade Code tab"
    - "Submission is completed before the deadline 2026-06-15 15:59"
  artifacts:
    - path: "docs/DEMO_VIDEO_SCRIPT.md"
      provides: "The script the human follows to record (built in Plan 02)"
    - path: "SUBMISSION.md"
      provides: "The checklist the human ticks through to push + submit (built in Plan 02)"
  key_links:
    - from: "human"
      to: "DoraHacks"
      via: "public repo link + demo video submitted per SUBMISSION.md"
      pattern: "SUBMISSION\\.md"
---

<objective>
Final gated checkpoint: present the three genuine HUMAN actions that complete DEMO-05 and DEMO-06, using the materials built in Plans 01-02. Nothing autonomous remains — this plan pauses for the human to (1) record + upload the demo video, (2) push the repo to a public remote + submit to DoraHacks, and (3) visually confirm the pharosscan "Verified" checkmark — all before the 2026-06-15 15:59 deadline.

Purpose: The build produced every material (WOW viz, README, video script, submission checklist). These last steps require a human (camera/screen recording, a DoraHacks login, creating a public remote, and a visual explorer check the read API can't confirm). This plan surfaces them as one blocking checkpoint so the phase closes only when the entry is actually submitted.
Output: A completed hackathon submission (human-performed); no repo files change.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/06-visualization-docs-submission/06-CONTEXT.md
@MAINNET_RESULT.md
@SUBMISSION.md
@docs/DEMO_VIDEO_SCRIPT.md
@README.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Human actions — record video, push + submit to DoraHacks, confirm Verified checkmark</name>
  <read_first>
    - SUBMISSION.md (the turnkey checklist with push commands + pre-filled DoraHacks fields + the Verified-checkmark line)
    - docs/DEMO_VIDEO_SCRIPT.md (the shot-by-shot script to record from)
    - MAINNET_RESULT.md (the verified address + tx hashes the submission references)
  </read_first>
  <what-built>
    Plans 01-02 produced everything submittable: the WOW visualization (web/index.html — opens offline, animates the real mainnet money-flow), the polished README.md, the recordable docs/DEMO_VIDEO_SCRIPT.md, and the turnkey SUBMISSION.md checklist with pre-filled DoraHacks form fields. The only remaining work is the three human-only actions below.
  </what-built>
  <how-to-verify>
    Work through SUBMISSION.md top to bottom:
    1. Record the demo video by following docs/DEMO_VIDEO_SCRIPT.md (open web/index.html, play the money-flow, show the real pharosscan invoke tx, then the 4337 batch), then upload it and capture the public video URL.
    2. Create a public repo on GitHub/GitLab/Bitbucket, then run the push commands in SUBMISSION.md (`git remote add origin <URL>` + `git push -u origin main`). Before pushing, confirm `.env` is NOT staged (it is gitignored) — no private keys leave the machine.
    3. Open https://www.pharosscan.xyz/address/0x31bE4C6B5711913D818e377ebd809d4397FF3c84 and visually confirm the green "Verified" checkmark on the Code tab (the carried-over Phase 5 residual — the read API cannot confirm this programmatically).
    4. Fill the DoraHacks submission form with the pre-filled fields from SUBMISSION.md (name Cascade, description, the public GitHub link, the demo/video link, instructions -> README Quickstart, framework = Anthropic Agent Skills + Foundry, track = Skill Hackathon) and submit.
    5. Confirm submission is complete BEFORE the deadline 2026-06-15 15:59.
  </how-to-verify>
  <acceptance_criteria>
    - Human confirms the video is recorded + uploaded and the public URL is captured.
    - Human confirms the repo is pushed to a public remote with no secrets committed (.env excluded).
    - Human confirms the pharosscan "Verified" checkmark is visible on the Cascade Code tab.
    - Human confirms the DoraHacks form is submitted before 2026-06-15 15:59.
  </acceptance_criteria>
  <resume-signal>Type "submitted" once the video is uploaded, the repo is public, the Verified checkmark is confirmed, and the DoraHacks form is submitted — or describe any blocker.</resume-signal>
</task>

</tasks>

<verification>
- This is a blocking human-verify checkpoint; completion is the human's confirmation that all four acceptance items are done before the deadline.
- No repo files are modified by this plan.
</verification>

<success_criteria>
The demo video is recorded + uploaded, the repo is public, the pharosscan Verified checkmark is visually confirmed, and the entry is submitted to DoraHacks before 2026-06-15 15:59 — completing DEMO-05 and DEMO-06 and the project.
</success_criteria>

<output>
Create `.planning/phases/06-visualization-docs-submission/06-03-SUMMARY.md` when done.
</output>
