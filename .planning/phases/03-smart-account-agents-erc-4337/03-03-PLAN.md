---
phase: 03-smart-account-agents-erc-4337
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - script/LiveBundle.s.sol
  - .planning/phases/03-smart-account-agents-erc-4337/LIVE_RESULT.md
autonomous: false
requirements: [AA-03]

must_haves:
  truths:
    - "A pre-flight gate reads the funded EOA balance and the node's accepted gas price, estimates the full live cost, and STOPS CLEANLY (writing a top-up request) rather than half-spending if it won't fit"
    - "IF (and only if) the budget fits: a smart account is deployed live, funded minimally, and ONE handleOps batches two invokes of the EXISTING live skills through the real EntryPoint v0.7 — visible on the explorer"
    - "No live skills are re-registered (skillCount stays 3); the live demo reuses the existing A->B->C tree"
    - "Every outcome (settled, or blocked-on-budget) is recorded in LIVE_RESULT.md with the explorer tx link or the exact top-up amount needed"
  artifacts:
    - path: "script/LiveBundle.s.sol"
      provides: "gated funded live step: pre-flight budget check, optional account deploy + funded handleOps(2 invokes)"
      contains: "pre-flight"
    - path: ".planning/phases/03-smart-account-agents-erc-4337/LIVE_RESULT.md"
      provides: "recorded live outcome — settled tx link OR blocked-on-budget top-up report"
      contains: "EntryPoint"
  key_links:
    - from: "script/LiveBundle.s.sol"
      to: "funded EOA 0x67680b09bB422cC510669bd5208D947066D4aeaE balance + node gas price"
      via: "pre-flight estimate × accepted gas price vs balance → proceed or STOP"
      pattern: "balance"
    - from: "script/LiveBundle.s.sol"
      to: "real EntryPoint 0x0000000071727De22E5E9d8BAf0edAc6f37da032"
      via: "handleOps(ops, beneficiary) live broadcast (only if gate passes)"
      pattern: "handleOps"
---

<objective>
OPTIONAL stronger on-chain evidence: deploy a smart account live on atlantic-testnet, fund it minimally, and submit ONE self-bundled `handleOps` that batches two invokes of the EXISTING live Cascade skills (skillCount==3, NO re-registration) through the real EntryPoint v0.7 — visible on the explorer.

THE PHASE'S REQUIREMENTS (AA-01/02/03) ARE ALREADY MET by Wave 2's fork proof against the real EntryPoint. This wave is bonus. The HONEST budget reality (03-RESEARCH.md §8): the node reports 10 gwei; a live 4337 demo is ~1.2-2.2M gas ≈ 0.012-0.022 PHRS, but only ~0.0043 PHRS remains. It fits ONLY if the node accepts ~1 gwei. So this plan is GATED: a pre-flight check must STOP CLEANLY and report a top-up need rather than half-spending.

Purpose: capture real on-chain evidence IF the budget allows, without risking a wasted partial spend.
Output: `script/LiveBundle.s.sol` (gated) + `LIVE_RESULT.md` recording either the settled explorer tx link or the precise top-up amount needed. Lists AA-03 as bonus coverage; not the sole coverer of any requirement.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-smart-account-agents-erc-4337/03-RESEARCH.md
@.planning/phases/03-smart-account-agents-erc-4337/03-CONTEXT.md
@src/Cascade.sol
@script/Invoke.s.sol
@test/SmartAccount.fork.t.sol

<interfaces>
<!-- Built in Waves 1-2. This live step reuses them and the same op-build/sign flow proven on the fork. -->

From src/aa/CascadeAccount.sol / AccountFactory.sol / IEntryPoint.sol (Wave 1) — same signatures as Wave 2.

Live constants (VERIFIED, chainId 688689):
- EntryPoint v0.7: 0x0000000071727De22E5E9d8BAf0edAc6f37da032
- Cascade:         0xd41C32562D0BE20D354120E1De11A91abC340F50  (skillCount()==3 — REUSE, do NOT re-register)
- Funded self-bundling EOA / top-up target: 0x67680b09bB422cC510669bd5208D947066D4aeaE
- Signer key: read PRIVATE_KEY from env (existing project pattern — NEVER echo or hardcode)

Footprint-shrink option (03-RESEARCH.md §8 mitigation 3): the factory deploy is the heaviest item (~600k-1.2M gas). For the LIVE step, deploy the account DIRECTLY (`new CascadeAccount(ep, owner)` or one CREATE2) and skip the factory — still satisfies AA-01 (account deployed) while cutting the biggest cost. The factory path is already proven in the Wave 2 fork test.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LiveBundle script with a STOP-clean pre-flight budget gate</name>
  <files>script/LiveBundle.s.sol</files>
  <read_first>
    - 03-RESEARCH.md §8 "Gas Reality" (the budget table + the 4 mitigations the planner MUST bake in), Pitfall 5 (gas-price blocker), §"Runtime State Inventory" (read live skill prices at runtime; invoke needs exact msg.value), §"Open Questions" 1+2 (gas floor; live prices)
    - script/Invoke.s.sol (project script env pattern: vm.envUint("PRIVATE_KEY"), vm.startBroadcast, --rpc-url)
    - src/aa/CascadeAccount.sol, src/aa/IEntryPoint.sol (Wave 1); test/SmartAccount.fork.t.sol (Wave 2 op-build/sign flow to mirror)
    - src/Cascade.sol (invoke requires msg.value == price; read prices for the two chosen skill ids at runtime)
  </read_first>
  <action>
    Create script/LiveBundle.s.sol. It MUST begin with a PRE-FLIGHT GATE before any broadcast:
    1. Read the self-bundling EOA balance (the broadcaster derived from PRIVATE_KEY) and read the two target skill prices on-chain at runtime (so msg.value is exact). The chosen skill ids and a GAS_PRICE_WEI (default 1 gwei = 1000000000) come from env with sensible defaults.
    2. Estimate total live cost = (account-deploy gas + handleOps-batch gas, use the §8 estimates as upper bounds, e.g. ~0.5M + ~0.5M) × GAS_PRICE_WEI, PLUS the native value the account must hold (prefund + p1 + p2).
    3. If `estimatedTotal > balance`: STOP CLEANLY — `console2.log` a clear "LIVE BLOCKED — top up 0x67680b09bB422cC510669bd5208D947066D4aeaE by <X> PHRS at <price> gwei" message and `revert`/return WITHOUT broadcasting anything. Never half-spend.
    4. If it fits: deploy the account DIRECTLY (`new CascadeAccount(IEntryPoint(EP), owner)` — skip the factory to shrink footprint per §8 mitigation 3; initCode then empty), send it the minimal native funding (prefund + p1 + p2), build the PackedUserOperation with callData = executeBatch([cascade,cascade],[p1,p2],[invoke(s1),invoke(s2)]), pack accountGasLimits/gasFees EXACTLY as in the fork test (use GAS_PRICE_WEI for both maxFee/maxPriority — legacy-safe per Assumption A5), hash via `ep.getUserOpHash(op)`, sign the eth-signed-prefixed hash, set signature, and `ep.handleOps(ops, payable(broadcaster))` inside `vm.startBroadcast(pk)`. Log the resulting balances and the EntryPoint/Cascade addresses. Read PRIVATE_KEY via env only; never echo it. NO call to Cascade.register anywhere (reuse the existing 3 skills).
    Keep the script driven entirely by env + --rpc-url (mirror Invoke.s.sol). It must COMPILE and the pre-flight path must be reachable without funds.
  </action>
  <verify>
    <automated>forge build 2>&1 | grep -q "Compiler run successful" && grep -qi "pre-flight\|preflight\|LIVE BLOCKED\|top up" script/LiveBundle.s.sol && grep -q "handleOps" script/LiveBundle.s.sol && ! grep -q "register(" script/LiveBundle.s.sol</automated>
  </verify>
  <acceptance_criteria>
    - `forge build` reports "Compiler run successful"
    - the script has a pre-flight gate that compares (estimated gas × gas price + required native value) against the EOA balance and STOPS (revert/return, no broadcast) when it doesn't fit, logging the top-up address 0x67680b09bB422cC510669bd5208D947066D4aeaE and the shortfall
    - skill prices are read on-chain at runtime (no hardcoded msg.value); `grep 'register(' script/LiveBundle.s.sol` finds NOTHING (no re-registration)
    - the live path deploys the account directly (no factory dependency) and calls `ep.handleOps(...)` exactly once with a two-call executeBatch
    - PRIVATE_KEY read from env (`grep 'envUint("PRIVATE_KEY")\|envUint(\"PRIVATE_KEY\")' script/LiveBundle.s.sol`); no hardcoded key
  </acceptance_criteria>
  <done>LiveBundle.s.sol compiles, contains a STOP-clean pre-flight budget gate, reuses the existing 3 live skills, and (only if budget fits) submits one handleOps batching two invokes via the real EntryPoint.</done>
</task>

<task type="auto">
  <name>Task 2: Run the gated live step (or record the budget block) into LIVE_RESULT.md</name>
  <files>.planning/phases/03-smart-account-agents-erc-4337/LIVE_RESULT.md</files>
  <read_first>
    - 03-RESEARCH.md §8 (budget verdict), §"Open Questions" 1 (try 1 gwei; if rejected, declare blocked)
    - script/LiveBundle.s.sol (Task 1)
  </read_first>
  <action>
    Execute the pre-flight by running the live script in a dry/gated manner against atlantic_testnet: first `cast balance 0x67680b09bB422cC510669bd5208D947066D4aeaE --rpc-url atlantic_testnet` and `cast gas-price --rpc-url atlantic_testnet` to capture the current numbers, then run `forge script script/LiveBundle.s.sol --rpc-url atlantic_testnet` (NO --broadcast first, to surface the pre-flight decision). 
    - IF the gate reports BLOCKED (expected at 10 gwei): do NOT broadcast. Write LIVE_RESULT.md recording: current balance, current gas price, estimated cost, the exact shortfall, and the top-up instruction for 0x67680b09bB422cC510669bd5208D947066D4aeaE. State clearly that AA-01/02/03 are ALREADY satisfied by the Wave 2 fork proof and that this live step is blocked only on budget (not on correctness).
    - IF the gate reports it FITS (e.g. node accepts 1 gwei): re-run with `--broadcast --legacy --gas-price <accepted>` to submit, then write LIVE_RESULT.md recording the handleOps tx hash, the explorer link (https://atlantic.pharosscan.xyz/tx/<hash>), the two skill ids/prices invoked, and the creator balance deltas confirming the batched payment settled.
    Do NOT attempt a partial spend. If the first (account-deploy) broadcast succeeds but funds run short before handleOps, record the partial state honestly in LIVE_RESULT.md.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-smart-account-agents-erc-4337/LIVE_RESULT.md && grep -qi "gas price\|gas-price" .planning/phases/03-smart-account-agents-erc-4337/LIVE_RESULT.md && grep -qi "blocked\|tx\|explorer\|0x" .planning/phases/03-smart-account-agents-erc-4337/LIVE_RESULT.md</automated>
  </verify>
  <acceptance_criteria>
    - LIVE_RESULT.md exists and records the captured balance + node gas price + cost estimate
    - the outcome is one of exactly two honest states: (a) BLOCKED — with the precise top-up amount + address and a note that fork proof already satisfies AA-01/02/03, or (b) SETTLED — with the handleOps tx hash, explorer link, and confirmed creator balance deltas
    - no partial/half-spend was performed silently; any partial state is recorded explicitly
    - skillCount on the live Cascade is still 3 (no re-registration occurred)
  </acceptance_criteria>
  <done>The live step was attempted through the gate; LIVE_RESULT.md honestly records either the budget block (with top-up details) or the settled on-chain batched UserOp (with explorer link + balance deltas).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the live outcome (or accept the fork proof)</name>
  <action>
    Pause for the user to review LIVE_RESULT.md and confirm the Phase 3 close — either the settled on-chain batched UserOp (explorer link) or the budget block (the Wave 2 fork proof already satisfies AA-01/02/03). See <how-to-verify> for the exact steps. No code changes in this task; it gates the phase on human confirmation.
  </action>
  <what-built>
    A gated live ERC-4337 demo: script/LiveBundle.s.sol (pre-flight budget gate + account deploy + one handleOps batching two invokes of the existing live skills) and LIVE_RESULT.md recording the outcome. AA-01/02/03 were ALREADY proven by the Wave 2 fork test against the real EntryPoint; this is bonus on-chain evidence that may be budget-blocked at 10 gwei.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/03-smart-account-agents-erc-4337/LIVE_RESULT.md`.
    2. If it reports SETTLED: open the explorer link (https://atlantic.pharosscan.xyz/tx/<hash>) and confirm the handleOps tx succeeded and the smart account paid two skills. Confirm the recorded creator balance deltas.
    3. If it reports BLOCKED: confirm the budget numbers look right (balance ~0.0043 PHRS, gas price too high) and decide whether to (a) accept the fork proof as the phase deliverable and proceed to Phase 4, or (b) top up 0x67680b09bB422cC510669bd5208D947066D4aeaE and re-run the live step. Either is a valid completion of Phase 3.
    4. Confirm the live Cascade still has skillCount()==3 (no re-registration): `cast call 0xd41C32562D0BE20D354120E1De11A91abC340F50 "skillCount()(uint256)" --rpc-url atlantic_testnet`.
  </how-to-verify>
  <resume-signal>Type "approved" to close Phase 3, or "top up and retry" if you funded the EOA and want the live step re-run.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Funded EOA (PRIVATE_KEY) → live EntryPoint | Real PHRS spent; the only place this phase touches funds |
| Pre-flight estimate → spend decision | A wrong estimate could authorize a spend that exceeds budget |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Denial of Service (self-inflicted) | live spend | mitigate | pre-flight gate STOPS before any broadcast if estimate × price + value > balance; deploy account directly (no factory) to shrink footprint; never half-spend |
| T-03-09 | Information Disclosure | PRIVATE_KEY in script | mitigate | read from env only (existing pattern); never echo, never hardcode, never commit |
| T-03-10 | Tampering | wrong msg.value vs live skill price | mitigate | read each skill price on-chain at runtime; Cascade reverts on mismatch (fail-safe) |
| T-03-SC | Tampering | package installs | accept | no installs; script uses forge-std + first-party Wave 1 contracts only |
</threat_model>

<verification>
- `forge build` — "Compiler run successful"
- `forge script script/LiveBundle.s.sol --rpc-url atlantic_testnet` (no --broadcast) surfaces the pre-flight decision without spending
- LIVE_RESULT.md records the honest outcome (BLOCKED with top-up details, or SETTLED with explorer link)
- Live Cascade skillCount()==3 unchanged
- Human-verify checkpoint approved
</verification>

<success_criteria>
- A STOP-clean pre-flight gate prevents any half-spend; budget block is reported with the exact top-up amount on 0x67680b09bB422cC510669bd5208D947066D4aeaE
- IF budget fits: one live handleOps batches two invokes of the existing skills via the real EntryPoint, visible on the explorer, recorded in LIVE_RESULT.md
- No re-registration (skillCount stays 3)
- Phase 3 is complete either way — the Wave 2 fork proof already satisfies AA-01/02/03; this wave is bonus on-chain evidence
</success_criteria>

<output>
Create `.planning/phases/03-smart-account-agents-erc-4337/03-03-SUMMARY.md` when done.
</output>
