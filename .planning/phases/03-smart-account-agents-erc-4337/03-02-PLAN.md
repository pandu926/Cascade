---
phase: 03-smart-account-agents-erc-4337
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - test/SmartAccount.fork.t.sol
autonomous: true
requirements: [AA-02, AA-03]

must_haves:
  truths:
    - "One self-bundled UserOperation, via the REAL Pharos EntryPoint v0.7, makes the smart account pay two skills in a single handleOps call"
    - "Two distinct creator balances rise after the batched op, summing exactly to the two skill prices"
    - "The op settles through the real EntryPoint at 0x0000000071727De22E5E9d8BAf0edAc6f37da032 (fork), self-bundled by an EOA calling handleOps — no external bundler"
    - "A UserOp signed by a non-owner reverts FailedOp(0, 'AA24...') — wrong signer is rejected"
    - "The smart-account invoke path credits creators identically to a direct EOA invoke (account-agnostic; Cascade.sol unchanged)"
  artifacts:
    - path: "test/SmartAccount.fork.t.sol"
      provides: "fork test proving the full self-bundled batch flow against the real EntryPoint + live Cascade"
      contains: "test_handleOps_settles_via_real_entrypoint"
  key_links:
    - from: "test/SmartAccount.fork.t.sol"
      to: "EntryPoint 0x0000000071727De22E5E9d8BAf0edAc6f37da032"
      via: "ep.getUserOpHash(op) + ep.handleOps(ops, beneficiary) on the fork"
      pattern: "handleOps"
    - from: "test/SmartAccount.fork.t.sol"
      to: "Cascade 0xd41C32562D0BE20D354120E1De11A91abC340F50"
      via: "executeBatch callData → Cascade.invoke{value} ×2"
      pattern: "invoke"
---

<objective>
Prove the killer claim end-to-end, locally, with ZERO funds: ONE self-bundled UserOperation makes the `CascadeAccount` pay TWO Cascade skills in a single `handleOps` call through the REAL Pharos EntryPoint v0.7 (via `--fork-url atlantic_testnet`), and two creator balances rise exactly as if an EOA had invoked twice. This is the PRIMARY proof of AA-02 (batched UserOp) and AA-03 (settles via the real EntryPoint, self-bundled), plus the account-agnostic parity (CORE-07) and a negative AA24 wrong-signer test.

Purpose: The headline technical risk (v0.7 packing/hashing/prefund/handleOps fidelity) is fully de-risked here against the real on-chain EntryPoint bytecode — no funds needed because `vm.deal` funds the counterfactual account on the fork. After this wave, AA-01/02/03 are all satisfied locally; the funded Wave 3 is bonus on-chain evidence only.
Output: `test/SmartAccount.fork.t.sol` green against the real EntryPoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-smart-account-agents-erc-4337/03-RESEARCH.md
@.planning/phases/03-smart-account-agents-erc-4337/03-CONTEXT.md
@src/Cascade.sol
@test/Demo.fork.t.sol

<interfaces>
<!-- Built in Wave 1 (plan 03-01). The fork test consumes these directly. -->

From src/aa/CascadeAccount.sol:
```solidity
function executeBatch(address[] calldata dest, uint256[] calldata value, bytes[] calldata func) external;
constructor(IEntryPoint anEntryPoint, address anOwner);
```
From src/aa/AccountFactory.sol:
```solidity
function createAccount(address owner, uint256 salt) external returns (address);
function getAddress(address owner, uint256 salt) external view returns (address);
```
From src/aa/IEntryPoint.sol:
```solidity
function handleOps(PackedUserOperation[] calldata ops, address payable beneficiary) external;
function getUserOpHash(PackedUserOperation calldata userOp) external returns (bytes32);
function getNonce(address sender, uint192 key) external view returns (uint256);
```
From src/Cascade.sol:
```solidity
function invoke(uint256 skillId) external payable;     // requires msg.value == skills[skillId].price
function balances(address) external view returns (uint256);
function skillCount() external view returns (uint256);
```

Live constants (VERIFIED on atlantic-testnet, chainId 688689):
- EntryPoint v0.7: 0x0000000071727De22E5E9d8BAf0edAc6f37da032
- Cascade:         0xd41C32562D0BE20D354120E1De11A91abC340F50  (skillCount()==3)

Packing (get EXACT — Wave 1 struct fields):
- accountGasLimits = bytes32((uint256(uint128(verificationGasLimit)) << 128) | uint128(callGasLimit))
- gasFees          = bytes32((uint256(uint128(maxPriorityFeePerGas)) << 128) | uint128(maxFeePerGas))
- signature = abi.encodePacked(r, s, v) over keccak256("\x19Ethereum Signed Message:\n32" ++ ep.getUserOpHash(op))
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fork test — happy path: one handleOps batches two invokes, two balances rise</name>
  <files>test/SmartAccount.fork.t.sol</files>
  <read_first>
    - 03-RESEARCH.md §"Code Examples" (the full "Build + sign + self-bundle in a Foundry fork test" example — this is the spec for the flow), §"Pattern 1/2" (packing + getUserOpHash), Pitfall 1/3/4 (prefund, initCode, verificationGasLimit when deploying)
    - test/Demo.fork.t.sol (project fork-test style: vm.createSelectFork(vm.rpcUrl("atlantic_testnet")), snapshot-before/after balance asserts)
    - src/aa/CascadeAccount.sol, src/aa/AccountFactory.sol, src/aa/PackedUserOperation.sol, src/aa/IEntryPoint.sol (Wave 1)
    - src/Cascade.sol (invoke requires msg.value == price; read live prices at runtime)
  </read_first>
  <behavior>
    - On a fork of atlantic_testnet, a freshly deployed AccountFactory yields a counterfactual sender address
    - A UserOp with initCode (factory ++ createAccount calldata) and callData = executeBatch([cascade,cascade],[p1,p2],[invoke(s1),invoke(s2)]), signed by the owner, settles via ep.handleOps from an EOA beneficiary
    - After handleOps: the account is deployed (sender.code.length > 0), and the two invoked skills' creator balances rose, summing to p1 + p2
    - Skill prices p1/p2 are READ from the live Cascade at runtime (not hardcoded), so msg.value matches exactly
  </behavior>
  <action>
    Create test/SmartAccount.fork.t.sol. In setUp (or the test), `vm.createSelectFork(vm.rpcUrl("atlantic_testnet"))`. Bind `IEntryPoint ep = IEntryPoint(0x0000000071727De22E5E9d8BAf0edAc6f37da032)` and `Cascade cascade = Cascade(0xd41C32562D0BE20D354120E1De11A91abC340F50)`. Make owner via `makeAddrAndKey("owner")`. Deploy `AccountFactory factory = new AccountFactory(ep)`; `sender = factory.getAddress(owner, 0)`. `vm.deal(sender, 1 ether)` to cover value + prefund (zero real funds). Choose two skill ids to invoke (parametrize via constants/env, default to invoking existing skill ids that have a payable price; read `cascade`-exposed price per the Cascade Skill struct — if no public price getter exists, read via the registered price the same way the demo computes it, or invoke the same priced skill twice). READ each chosen skill's price at runtime so `val[i]` is exact. Build initCode = `abi.encodePacked(address(factory), abi.encodeWithSelector(AccountFactory.createAccount.selector, owner, uint256(0)))`. Build callData = `executeBatch([cascade,cascade],[p1,p2],[invoke(s1),invoke(s2)])`. Build the PackedUserOperation: nonce = `ep.getNonce(sender,0)`, accountGasLimits packed with a GENEROUS verificationGasLimit (≥400k, covers CREATE2 deploy inside validation per Pitfall 4) and callGasLimit (~300k), preVerificationGas ~60k, gasFees packed (1 gwei/1 gwei; if fork basefee is 0/odd, set maxFee==maxPriority per Assumption A5). Hash via `ep.getUserOpHash(op)` (authoritative), sign the eth-signed-prefixed hash with `vm.sign(pk, ethHash)`, set `op.signature = abi.encodePacked(r,s,v)`. Snapshot the two creators' balances, call `ep.handleOps(ops, payable(address(this)))`, assert both rose and Σ deltas == p1+p2, and `sender.code.length > 0`. Write the test FIRST and confirm it fails (RED) before the implementation is correct, then iterate to green.
  </action>
  <verify>
    <automated>forge test --fork-url atlantic_testnet --match-path test/SmartAccount.fork.t.sol --match-test test_smartAccount_batches_two_invokes -vv 2>&1 | grep -qE "0 failed" && grep -q "handleOps" test/SmartAccount.fork.t.sol && grep -q "0x0000000071727De22E5E9d8BAf0edAc6f37da032" test/SmartAccount.fork.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --fork-url atlantic_testnet --match-path test/SmartAccount.fork.t.sol --match-test test_smartAccount_batches_two_invokes` shows 0 failed
    - the test calls `ep.handleOps(...)` exactly once and asserts TWO creator balances rose with Σ == p1 + p2 (read prices at runtime, not hardcoded)
    - asserts `sender.code.length > 0` after the op (account deployed via initCode during validation)
    - uses `ep.getUserOpHash(op)` for the hash (authoritative) and the eth-signed prefix for the signature — matching the Wave 1 account's recover
    - `grep '0x0000000071727De22E5E9d8BAf0edAc6f37da032' test/SmartAccount.fork.t.sol` present (real EntryPoint, not a local copy)
  </acceptance_criteria>
  <done>The happy-path fork test passes against the real EntryPoint: one self-bundled handleOps batches two invokes and two creator balances rise summing to the two prices. AA-02 + AA-03 proven locally with zero funds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fork tests — negative AA24 (wrong signer) + account-agnostic parity (CORE-07)</name>
  <files>test/SmartAccount.fork.t.sol</files>
  <read_first>
    - 03-RESEARCH.md Pitfall 2 (AA24 signature error), §"State of the Art" (validateUserOp returns 1 on bad sig — handleOps surfaces FailedOp), §"Code Examples" (parity assertion: assertGt(cascade.balances(creator), before))
    - test/SmartAccount.fork.t.sol (Task 1 — reuse its op-builder helper)
    - src/Cascade.sol (EOA invoke path to compare against)
  </read_first>
  <behavior>
    - A UserOp identical to the happy path but signed by a NON-owner key causes ep.handleOps to revert with FailedOp(0, "AA24...") — the wrong signer is rejected by validateUserOp returning 1
    - Invoking a single skill once via the smart account credits the same creator(s) by the same amount as a direct EOA invoke of that skill (parity) — Cascade.sol is unchanged and treats both senders identically
  </behavior>
  <action>
    Add two tests to test/SmartAccount.fork.t.sol. (a) `test_handleOps_rejects_wrong_signer`: build the same op but sign with a second key from `makeAddrAndKey("attacker")`; `vm.expectRevert` on the FailedOp selector/AA24 reason (use the EntryPoint's FailedOp error shape — expectRevert with the encoded selector, or expectRevert without data if the exact bytes are hard to match, then assert balances did NOT rise). (b) `test_account_agnostic_parity`: snapshot a creator's balance; perform ONE smart-account invoke of skill S via a single-element executeBatch (or execute) through handleOps and record the delta; on a fresh fork (or fresh creator snapshot), `vm.deal` an EOA and call `cascade.invoke{value:price}(S)` directly and record that delta; assert the two deltas are EQUAL — proving the account-agnostic claim that Cascade credits identically regardless of caller type. Extract a shared `_buildSignedOp(...)` helper from Task 1 to avoid duplication. Write failing first, then green.
  </action>
  <verify>
    <automated>forge test --fork-url atlantic_testnet --match-path test/SmartAccount.fork.t.sol -vv 2>&1 | grep -qE "0 failed" && grep -q "AA24\|FailedOp\|expectRevert" test/SmartAccount.fork.t.sol && grep -q "parity" test/SmartAccount.fork.t.sol</automated>
  </verify>
  <acceptance_criteria>
    - `forge test --fork-url atlantic_testnet --match-path test/SmartAccount.fork.t.sol` shows 0 failed for the WHOLE file (Task 1 + both new tests)
    - the negative test proves a wrong-signer op does NOT settle (handleOps reverts / FailedOp AA24, and the target creator balance is unchanged)
    - the parity test asserts the smart-account invoke delta EQUALS the direct-EOA invoke delta for the same skill (CORE-07 account-agnostic)
    - a shared signed-op builder helper is used by multiple tests (no copy-paste of the packing/signing block)
  </acceptance_criteria>
  <done>Negative AA24 test and account-agnostic parity test pass on the fork. The phase's AA-01/02/03 + CORE-07 are now fully proven locally against the real EntryPoint with zero funds.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test EOA → real EntryPoint (fork) | The self-bundling EOA submits ops; EntryPoint validates against the account |
| Owner key vs attacker key → UserOp signature | Only the owner's signature must produce a settling op |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Spoofing/Tampering | UserOp signature (fork) | mitigate | negative test asserts a non-owner-signed op reverts FailedOp AA24 — wrong signer cannot settle |
| T-03-06 | Tampering | encoding fidelity (packing/hash) | mitigate | hash via real `ep.getUserOpHash`; if packing/prefix wrong, handleOps reverts on the fork — test failure is the detector before any spend |
| T-03-07 | Replay | EntryPoint nonce | accept | nonce read via `ep.getNonce(sender,0)`; each test op is single-use on an ephemeral fork |
| T-03-SC | Tampering | package installs | accept | no installs — test uses only forge-std + Wave 1 first-party contracts; fork uses real on-chain EntryPoint bytecode (canonical) |
</threat_model>

<verification>
- `forge test --fork-url atlantic_testnet --match-path test/SmartAccount.fork.t.sol` — 0 failed (requires RPC egress to atlantic_testnet)
- `forge test` — full suite (Phase 1 unit/fork + Wave 1 unit + this) green
- The fork test references the REAL EntryPoint address and calls handleOps (grep-verifiable)
- Skill prices are read at runtime (no hardcoded msg.value that could mismatch the live price)
</verification>

<success_criteria>
- One self-bundled `handleOps` batches two `invoke`s via the real EntryPoint v0.7 on the fork; two creator balances rise summing to the two prices
- Wrong-signer op is rejected (FailedOp AA24); balances unchanged
- Smart-account invoke credits creators identically to the EOA path (CORE-07 parity)
- AA-02 + AA-03 fully proven locally with zero funds; AA-01 (factory deploy) exercised via initCode
</success_criteria>

<output>
Create `.planning/phases/03-smart-account-agents-erc-4337/03-02-SUMMARY.md` when done.
</output>
