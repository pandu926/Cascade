---
phase: 01-recursive-royalty-core
plan: 03
type: execute
wave: 3
depends_on: [01-02]
files_modified:
  - .planning/phases/01-recursive-royalty-core/DEMO_RESULT.md
autonomous: false
requirements: [DEMO-01, DEMO-02]
must_haves:
  truths:
    - "Cascade.sol is deployed and live on atlantic-testnet at a recorded address"
    - "The A->B->C toy tree (3 skills) is registered on-chain (DEMO-01)"
    - "One paid invoke of the top skill raises three distinct creator balances on-chain in depth-proportion (DEMO-02)"
    - "At least one creator successfully claims their accrued royalty to their own wallet on-chain"
    - "Every step is verifiable on the testnet explorer via tx hashes"
  artifacts:
    - path: ".planning/phases/01-recursive-royalty-core/DEMO_RESULT.md"
      provides: "recorded deployed address, skill ids, all tx hashes, before/after balances, explorer links"
      contains: "0x"
  key_links:
    - from: "DemoTree.s.sol broadcast"
      to: "atlantic-testnet (chainId 688689)"
      via: "forge script --broadcast --rpc-url atlantic_testnet"
      pattern: "broadcast"
---

<objective>
Deploy `Cascade.sol` live to atlantic-testnet and run the on-chain A→B→C demo: one paid invoke raises three creator balances, and a creator claims — all verifiable on the explorer. This is the ONLY plan that spends real testnet PHRS.

Purpose: Convert the locally-proven royalty engine into a live, submittable on-chain demonstration (DEMO-01 + DEMO-02) — the proof point of the whole Phase 1 milestone.
Output: Live deployed contract + on-chain tree + invoke + claim, with all tx hashes and balance deltas recorded in `DEMO_RESULT.md`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-recursive-royalty-core/01-CONTEXT.md
@.planning/phases/01-recursive-royalty-core/SKELETON.md
@.planning/phases/01-recursive-royalty-core/01-02-SUMMARY.md

<interfaces>
<!-- Live network — atlantic-testnet -->
<!-- rpc: https://atlantic.dplabs-internal.com  chainId: 688689  token: PHRS -->
<!-- explorer: https://atlantic.pharosscan.xyz/ -->
<!-- Wallet (funded ~0.01 PHRS): key in gitignored .env as PRIVATE_KEY -->
<!-- Budget: 1 deploy + 3 registers + 1 invoke + 3 claims ≈ 2M gas @ ~10 gwei -->
<!-- foundry.toml rpc_endpoints key: atlantic_testnet -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pre-flight — confirm wallet balance, gas budget, and clean local suite</name>
  <files>.planning/phases/01-recursive-royalty-core/DEMO_RESULT.md</files>
  <read_first>
    - .env (read PRIVATE_KEY only to derive address — DO NOT print the key value)
    - foundry.toml (rpc_endpoints)
    - .planning/phases/01-recursive-royalty-core/01-02-SUMMARY.md (demo numbers to reproduce live)
  </read_first>
  <action>
    Confirm readiness before spending funds. Derive the wallet address with `cast wallet address --private-key $PRIVATE_KEY` (do NOT echo the key). Query balance: `cast balance <addr> --rpc-url https://atlantic.dplabs-internal.com` and confirm it is > 0. Estimate the demo cost: run `forge test --gas-report` locally and confirm the deploy + 3 registers + 1 invoke + 3 claims gas sum fits within the available balance at ~10 gwei (≈ 2M gas budget). Re-run the full local suite (`forge test`) to confirm 0 failed before any live tx. Start `DEMO_RESULT.md` recording: wallet address, balance, chosen demo skill prices/shares, and estimated gas. If balance is insufficient for the estimated cost, STOP and report the shortfall (do not broadcast).
  </action>
  <verify>
    <automated>cd /root/pharos && forge test 2>&1 | grep -qE "[0-9]+ passed; 0 failed" && cast balance $(cast wallet address --private-key $(grep -m1 PRIVATE_KEY .env | cut -d= -f2)) --rpc-url https://atlantic.dplabs-internal.com 2>&1 | grep -qE "[1-9]" && echo PREFLIGHT-OK</automated>
  </verify>
  <acceptance_criteria>
    - Full local `forge test` reports 0 failed.
    - Wallet balance query returns a non-zero value.
    - Estimated demo gas cost ≤ available balance at ~10 gwei; `DEMO_RESULT.md` records address, balance, and estimate.
    - The PRIVATE_KEY value is never printed to logs.
  </acceptance_criteria>
  <done>Funding, gas budget, and a green local suite are confirmed; safe to spend testnet PHRS.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy Cascade and register the A→B→C tree live on atlantic-testnet (DEMO-01)</name>
  <files>.planning/phases/01-recursive-royalty-core/DEMO_RESULT.md</files>
  <read_first>
    - script/Deploy.s.sol, script/DemoTree.s.sol, script/Register.s.sol (broadcast scripts)
    - .planning/phases/01-recursive-royalty-core/DEMO_RESULT.md (pre-flight data)
  </read_first>
  <action>
    Broadcast the deploy and tree registration to atlantic-testnet using the wave-2 scripts with `--broadcast --rpc-url atlantic_testnet` (or the explicit RPC URL) and `--legacy` if the chain requires legacy gas; set a conservative `--gas-price` (~10 gwei) to stay within budget. Deploy `Cascade` (record the address), then register the three skills A→B→C with the demo prices/shares proven locally (record each returned skill id and tx hash). Prefer running `DemoTree.s.sol` if it deploys+registers in one broadcast; otherwise run Deploy then Register three times. Append to `DEMO_RESULT.md`: deployed address, three skill ids, each register tx hash, and explorer links (https://atlantic.pharosscan.xyz/tx/<hash>). Verify the deployment with `cast code <addr> --rpc-url ...` returning non-empty bytecode and `cast call <addr> "skillCount()(uint256)"` returning 3.
  </action>
  <verify>
    <automated>cd /root/pharos && grep -qE "0x[a-fA-F0-9]{40}" .planning/phases/01-recursive-royalty-core/DEMO_RESULT.md && grep -ciE "tx|hash" .planning/phases/01-recursive-royalty-core/DEMO_RESULT.md | grep -qvx 0 && echo DEPLOY-RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - `cast code <deployed addr>` returns non-empty bytecode on atlantic-testnet.
    - `cast call <addr> "skillCount()(uint256)"` returns 3 (three skills registered) — DEMO-01.
    - `DEMO_RESULT.md` records the deployed address, three skill ids, register tx hashes, and explorer links.
    - Spend stayed within the gas budget (remaining balance still > 0).
  </acceptance_criteria>
  <done>Cascade is live on atlantic-testnet with the A→B→C tree registered on-chain (DEMO-01), all artifacts recorded.</done>
</task>

<task type="auto">
  <name>Task 3: Live invoke + claim — three balances rise on-chain, one creator withdraws (DEMO-02)</name>
  <files>.planning/phases/01-recursive-royalty-core/DEMO_RESULT.md</files>
  <read_first>
    - script/Invoke.s.sol, script/Claim.s.sol
    - .planning/phases/01-recursive-royalty-core/DEMO_RESULT.md (deployed address + skill ids)
  </read_first>
  <action>
    Read the three creator balances BEFORE via `cast call <addr> "balances(address)(uint256)" <creator>` for each creator; record them. Broadcast `invoke{value: price}(C)` against the live contract (`Invoke.s.sol --broadcast --rpc-url atlantic_testnet`, ~10 gwei). After confirmation, read the three balances AFTER and record the deltas — assert all three rose and that they match the depth-proportional split (and Σ deltas == price), reproducing the local fork result on-chain. Then broadcast `claim()` from at least one creator (`Claim.s.sol`) and confirm that creator's wallet balance increased and their accrued balance returned to 0 via `cast call`. Append to `DEMO_RESULT.md`: invoke tx hash, before/after balances + deltas for all three creators, claim tx hash, and explorer links. This is the killer-demo proof (DEMO-02).
  </action>
  <verify>
    <automated>cd /root/pharos && grep -qiE "before|after|delta" .planning/phases/01-recursive-royalty-core/DEMO_RESULT.md && grep -qE "0x[a-fA-F0-9]{64}" .planning/phases/01-recursive-royalty-core/DEMO_RESULT.md && echo DEMO-RECORDED</automated>
  </verify>
  <acceptance_criteria>
    - One on-chain `invoke` tx (hash recorded) raises all three creator balances; recorded deltas are depth-proportional and Σ deltas == price (DEMO-02).
    - At least one creator's `claim()` tx (hash recorded) increases their wallet and zeroes their accrued balance.
    - `DEMO_RESULT.md` contains before/after balances, all tx hashes (64-hex), and explorer links.
  </acceptance_criteria>
  <done>One live paid invoke fanned royalties to three creators on-chain and a creator claimed — DEMO-02 demonstrated and fully recorded.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Cascade is deployed live to atlantic-testnet; the A→B→C tree is registered; one paid invoke raised three creator balances on-chain; and a creator claimed their royalty. All addresses, skill ids, tx hashes, and before/after balances are recorded in `.planning/phases/01-recursive-royalty-core/DEMO_RESULT.md`. This step spent real testnet PHRS.
  </what-built>
  <how-to-verify>
    1. Open `.planning/phases/01-recursive-royalty-core/DEMO_RESULT.md` and note the deployed address + tx hashes.
    2. Visit the contract on the explorer: https://atlantic.pharosscan.xyz/address/<deployed-address> — confirm it exists and has transactions.
    3. Open the invoke tx: https://atlantic.pharosscan.xyz/tx/<invoke-hash> — confirm it succeeded and emitted Invoked + three RoyaltyAccrued events.
    4. Confirm the recorded before/after balances show all three creators rose, proportional to depth, summing to the invoke price.
    5. Open the claim tx and confirm the creator's withdrawal succeeded.
  </how-to-verify>
  <resume-signal>Type "approved" if the on-chain demo checks out, or describe what looks wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → broadcast | Funded `PRIVATE_KEY` signs real testnet transactions |
| forge → atlantic-testnet RPC | Live, irreversible (on-chain) state changes spending PHRS |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-11 | Information disclosure | key leak during broadcast | mitigate | Address derived without printing key; key only read via env; never echoed. |
| T-01-12 | Denial of Service | out-of-gas / insufficient funds mid-demo | mitigate | Pre-flight balance + gas-budget check (Task 1) gates spend; conservative ~10 gwei; small demo price; STOP if short. |
| T-01-13 | Tampering | wrong network broadcast | mitigate | Explicit `--rpc-url atlantic_testnet`; verify chainId 688689 before broadcast; mainnet never targeted. |
| T-01-14 | Repudiation | demo unverifiable | mitigate | All tx hashes + explorer links recorded; blocking human-verify checkpoint confirms on-chain. |
| T-01-SC | Tampering | dependency supply chain | accept | No package installs; reuses 01-01/01-02 artifacts only. |
</threat_model>

<verification>
- Pre-flight: green local suite + non-zero balance + gas budget fits.
- `cast code` non-empty and `skillCount()` == 3 on atlantic-testnet (DEMO-01).
- One invoke raises three balances proportionally, Σ deltas == price; a claim succeeds (DEMO-02).
- All artifacts recorded with explorer links; human verifies on-chain.
</verification>

<success_criteria>
A single paid invoke on the live atlantic-testnet A→B→C tree visibly raises three creator balances proportional to depth, a creator claims their royalty, and everything is verifiable on the explorer — the Phase 1 milestone proven end-to-end on-chain.
</success_criteria>

<output>
Create `.planning/phases/01-recursive-royalty-core/01-03-SUMMARY.md` when done.
</output>
