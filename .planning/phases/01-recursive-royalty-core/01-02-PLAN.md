---
phase: 01-recursive-royalty-core
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - script/Deploy.s.sol
  - script/Register.s.sol
  - script/Invoke.s.sol
  - script/Claim.s.sol
  - script/DemoTree.s.sol
  - test/Demo.fork.t.sol
  - .env.example
autonomous: true
requirements: [SKILL-04, DEMO-01, DEMO-02]
must_haves:
  truths:
    - "A Deploy script deploys Cascade and prints the deployed address"
    - "A Register script registers a skill (price + dep ids + dep shares) from env-provided params"
    - "An Invoke script invokes a skill with exact msg.value and reports success"
    - "A Claim script withdraws the caller's accrued balance"
    - "A DemoTree script registers the A->B->C tree, invokes C once, and prints all three creator balances before/after"
    - "The whole demo flow runs locally (anvil/fork) with zero testnet funds before any live spend"
  artifacts:
    - path: "script/Deploy.s.sol"
      provides: "forge script to deploy Cascade"
      contains: "new Cascade"
    - path: "script/DemoTree.s.sol"
      provides: "scripted A->B->C register + invoke + balance print (drives DEMO-02)"
    - path: "test/Demo.fork.t.sol"
      provides: "local end-to-end run of the demo flow via the scripts/contract, zero funds"
      contains: "function test"
    - path: "script/Register.s.sol"
      provides: "register forge script (SKILL-04)"
    - path: "script/Invoke.s.sol"
      provides: "invoke forge script (SKILL-04)"
    - path: "script/Claim.s.sol"
      provides: "claim forge script (SKILL-04)"
  key_links:
    - from: "script/DemoTree.s.sol"
      to: "src/Cascade.sol"
      via: "register + invoke calls on deployed instance"
      pattern: "\\.register\\(|\\.invoke\\("
    - from: "test/Demo.fork.t.sol"
      to: "script/DemoTree.s.sol"
      via: "runs the demo flow logic locally"
      pattern: "DemoTree|Cascade"
---

<objective>
Build the register / invoke / claim forge scripts (SKILL-04) plus a scripted A→B→C demo, and prove the full demo flow runs locally with zero testnet funds — so the only thing left for the funded wave is pointing the same scripts at the live RPC.

Purpose: Forge scripts are the agent-facing execution surface the official engine convention requires, and the DemoTree script is the killer-demo driver (one invoke → three balances rise). Running it all locally first de-risks the single funded step.
Output: `script/{Deploy,Register,Invoke,Claim,DemoTree}.s.sol`, a `.env.example`, and a passing `test/Demo.fork.t.sol` that exercises the demo end-to-end locally.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-recursive-royalty-core/01-CONTEXT.md
@.planning/phases/01-recursive-royalty-core/SKELETON.md

<interfaces>
<!-- From src/Cascade.sol (built in 01-01). Use these signatures directly — no exploration needed. -->
function register(uint256 price, uint256[] calldata depIds, uint256[] calldata depShares) returns (uint256 id);
function invoke(uint256 skillId) payable;
function claim() returns (uint256);
function balances(address) view returns (uint256);
function skillCount() view returns (uint256);
event SkillRegistered(uint256 indexed id, address indexed creator, uint256 price);
event Invoked(uint256 indexed skillId, address indexed payer, uint256 amount);
event RoyaltyAccrued(uint256 indexed skillId, address indexed creator, uint256 amount);
event Claimed(address indexed creator, uint256 amount);
<!-- foundry.toml rpc_endpoints: atlantic_testnet, mainnet. Secret: PRIVATE_KEY in gitignored .env. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Deploy / Register / Invoke / Claim forge scripts (SKILL-04)</name>
  <files>script/Deploy.s.sol, script/Register.s.sol, script/Invoke.s.sol, script/Claim.s.sol, .env.example</files>
  <read_first>
    - src/Cascade.sol (public API to call)
    - .planning/phases/01-recursive-royalty-core/01-CONTEXT.md (exact-payment, share/dep semantics)
    - /tmp/pharos-skill-engine/assets/networks.json (network names/rpc to mirror)
    - foundry.toml (rpc_endpoints keys: atlantic_testnet, mainnet)
  </read_first>
  <action>
    Create four forge scripts (forge-std `Script`), each reading `PRIVATE_KEY` via `vm.envUint("PRIVATE_KEY")` and wrapping calls in `vm.startBroadcast`/`stopBroadcast`:
    - `Deploy.s.sol`: deploys `new Cascade()` and `console2.log`s the deployed address.
    - `Register.s.sol`: reads `CASCADE` (address), `PRICE` (uint, wei), `DEP_IDS` and `DEP_SHARES` (parse via `vm.envUint`/`vm.envUint` array helpers, or comma-split env strings), calls `register(...)`, logs the returned skill id.
    - `Invoke.s.sol`: reads `CASCADE`, `SKILL_ID`, and the skill's `price` (read via `Cascade(addr)` getter or `INVOKE_VALUE` env), calls `invoke{value: price}(skillId)`.
    - `Claim.s.sol`: reads `CASCADE`, calls `claim()`, logs the withdrawn amount.
    Create `.env.example` documenting every env var WITHOUT secrets: `PRIVATE_KEY=`, `RPC_URL=https://atlantic.dplabs-internal.com`, `CASCADE=`, `PRICE=`, `DEP_IDS=`, `DEP_SHARES=`, `SKILL_ID=`. Scripts must target the network via `--rpc-url` flag at run time (do not hardcode RPC). Keep scripts thin and KISS.
  </action>
  <verify>
    <automated>cd /root/pharos && forge build 2>&1 | grep -q "Compiler run successful" && for s in Deploy Register Invoke Claim; do test -f "script/$s.s.sol" || { echo "MISSING $s"; exit 1; }; done && test -f .env.example && echo SCRIPTS-OK</automated>
  </verify>
  <acceptance_criteria>
    - All four scripts compile (`forge build` successful).
    - Each script reads `PRIVATE_KEY` from env and uses `vm.startBroadcast`.
    - No RPC URL hardcoded inside scripts (resolved via `--rpc-url`); grep scripts shows no literal `dplabs-internal` or `rpc.pharos`.
    - `.env.example` exists and contains no real key value.
  </acceptance_criteria>
  <done>The register/invoke/claim/deploy forge scripts exist, compile, and read params from env — satisfying SKILL-04's "forge scripts for register/invoke/claim".</done>
</task>

<task type="auto">
  <name>Task 2: DemoTree script + local fork test proving one invoke raises three balances (DEMO-01/02, local)</name>
  <files>script/DemoTree.s.sol, test/Demo.fork.t.sol</files>
  <read_first>
    - script/Deploy.s.sol, script/Register.s.sol, script/Invoke.s.sol (patterns to reuse)
    - src/Cascade.sol
    - test/Cascade.t.sol (existing assertions/math to mirror)
    - .planning/phases/01-recursive-royalty-core/01-CONTEXT.md (A→B→C demo design + before/after balance print)
  </read_first>
  <action>
    Write `script/DemoTree.s.sol`: a single broadcastable script that deploys Cascade (or reads `CASCADE` if set), registers the A→B→C tree (A leaf price 0; B deps [A] with a share; C deps [B] with a share and price e.g. 0.001 ether — KEEP the demo price small to fit the ~0.01 PHRS budget), `console2.log`s the three creator balances BEFORE, invokes C once with exact value, then logs the three balances AFTER and the deltas. This is the DEMO-02 driver. Use distinct creator addresses derived deterministically OR accept three creator addresses via env for the live run.
    Write `test/Demo.fork.t.sol`: a forge test that performs the identical demo flow locally (no broadcast, plain forge EVM) — deploy Cascade, register A→B→C, snapshot three balances, invoke C, assert all three rose, assert deltas match the depth-proportional share math, assert Σ deltas == price. This proves the whole demo works with ZERO testnet funds before the funded wave.
    Keep the demo numbers identical between the script and the fork test so the live run reproduces the local result.
  </action>
  <verify>
    <automated>cd /root/pharos && forge build 2>&1 | grep -q "Compiler run successful" && forge test --match-path test/Demo.fork.t.sol -vv 2>&1 | grep -qE "[0-9]+ passed; 0 failed" && echo DEMO-LOCAL-OK</automated>
  </verify>
  <acceptance_criteria>
    - `script/DemoTree.s.sol` compiles and registers A→B→C then invokes C, logging before/after balances for all three creators.
    - `test/Demo.fork.t.sol` passes: three balances rise, deltas are depth-proportional, Σ deltas == price.
    - Demo price is small (≤ ~0.001 ether) to fit the live ~0.01 PHRS budget.
    - `forge test` whole suite still 0 failed.
  </acceptance_criteria>
  <done>The killer-demo flow (one invoke → three balances rise proportionally) is scripted and proven locally with zero funds; the live wave only needs to point it at testnet.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env → forge script | `PRIVATE_KEY` (funded testnet wallet) read by broadcast scripts |
| script → RPC | Broadcast tx sent to a network endpoint chosen at run time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Information disclosure | private key leak | mitigate | Key only in gitignored `.env`; `.env.example` carries no value; scripts read via `vm.envUint`, never log the key. |
| T-01-09 | Tampering | wrong network / mainnet misfire | mitigate | RPC resolved via explicit `--rpc-url` flag, not hardcoded; demo defaults to atlantic-testnet. |
| T-01-10 | Repudiation | demo result not reproducible | mitigate | Identical demo numbers in fork test and live script; before/after balances logged. |
| T-01-SC | Tampering | dependency supply chain | accept | No new package installs; only forge-std from 01-01. |
</threat_model>

<verification>
- All scripts compile; demo fork test passes (3 balances rise, Σ == price) with zero funds.
- No hardcoded RPC or key in scripts; `.env.example` present and clean.
- Demo price small enough for the live budget.
</verification>

<success_criteria>
The register/invoke/claim/deploy forge scripts and the A→B→C demo driver exist and run end-to-end locally, proving DEMO-01/DEMO-02 logic before any testnet spend.
</success_criteria>

<output>
Create `.planning/phases/01-recursive-royalty-core/01-02-SUMMARY.md` when done.
</output>
